# 需求追踪矩阵

更新日期：2026-08-15（flash 复审闭环：P1 身份/世代/独立模式/数据故障与冲突恢复）

本矩阵以工作区根目录的 `详细任务.txt` 为总验收入口，并以较晚的 `.workflow/.analysis/ANL-多节点云酒馆总控方案复盘优化-2026-08-07/discussion.md` 问答结论覆盖早期方案冲突。状态只依据当前代码和已执行测试，不把 README 声明、静态页面或正常演示当作完成证据。

状态定义：

- `缺失`：没有可执行实现，或只有注释/占位。
- `错误`：已有实现与确认需求、安全边界或故障语义冲突。
- `部分`：存在可复用代码，但缺少关键状态、事务、认证或异常闭环。
- `待验证`：实现看似存在，但尚无覆盖目标范围的测试或运行证据。
- `完成`：对应实现和验收证据均已满足；只有代码、异常矩阵与实际运行证据同时闭环的条目才可使用。

## 核心需求矩阵

| ID | 已确认需求 | Controller | Agent | SillyTavern 最小改造 | 前端 | PostgreSQL 事实状态 | 必须测试 | 当前证据与判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R01 | 总控只管理统一身份、调度、用户私有副本、恢复、容量、审计和告警；节点论坛、公告、公共角色、Gemini、全局插件保持独立 | 需按边界输出用户副本清单和兼容门禁 | 仅允许用户目录和精确账号元数据能力 | 暴露用户目录/账号边界，不复制节点全局数据 | 用户与管理员视图分层 | `global_users`、`node_accounts`、副本与节点状态分开 | 全局目录永不进入快照；用户插件数据保留 | 已挂载签名 loopback adapter：扫描精确账号、OAuth、管理员、用户目录摘要，并把酒馆版本、固定能力与本机插件目录组成兼容指纹；账号库存以 node-keyed revision、250 条 durable command page 和 10,000 总量硬边界传递。磁盘 fallback 现在只承认酒馆规范 handle，并显式排除当前/兼容节点全局根 `_storage/_uploads/_cache/_exports/_webpack/_global/_stcontrol/default-template/default-user/announcements/forum_data/public_characters/system-monitor/backups`，未知下划线、空格、隐藏目录失败关闭；同一边界同时用于库存、活动与容量。真实文件测试证明用户聊天、`secrets.json` 和两类用户私有插件数据完整进入 manifest/archive/原子发布，源/目标全部节点全局兄弟目录均不进入快照且目标内容逐字节不变，旧用户目录只在校验成功后替换。Controller 的身份/副本/节点事实保持分表且不持久节点全局正文，R01 验收闭环，`完成` |
| R02 | 受控节点关闭原生密码登录、OAuth、注册、改密、删号等入口；独立灾难模式仅允许已有用户登录 | 管理节点模式和恢复排空状态 | 持久化并执行固定模式切换命令 | 默认关闭的 federation adapter；请求级模式守卫 | 给出受控/独立/排空提示 | 节点控制模式、世代、切换原因、时间 | 所有原生入口绕过测试；模式重启恢复 | adapter 已挂载到真实 server，公开/私有/OAuth/管理员 Router 均在任何业务 handler 前安装请求级守卫；浏览器 `/login`、`/register` 和 LinuxDo 兼容 `/oauth` 入口也复用同一守卫。受控、短失联和 draining 均不开放原生登录；只有 independent 的显式密码 POST 可进入，并继续要求已有账号、真实密码、跨 Agent 最后活动归属/用户确认。单用户无密码、Authelia/Authentik trusted header 与 per-user Basic 等隐式 auto-login 在启用 stcontrol 时全部失败关闭，不能绕过归属证明；注册、恢复、改密、删号、OAuth 首次/回调、原生管理员变更在 independent 中仍关闭。模式/世代/待同步事实原子持久化及进程重启证据既有，实际 Router 全枚举和四模式矩阵已通过，R02 验收闭环，`完成` |
| R03 | 登录凭证不得进 URL；票据必须校验算法、`typ`、`iss`、`aud`、`sub`、用户 UUID、节点、会话、活动世代、`iat/nbf/exp`、`kid`、一次性 `jti` | 签发 POST/短码交换，原子消费并绑定节点身份 | 不接触浏览器长期令牌 | POST body 或一次性短码落地；严格 no-store/referrer 策略 | 表单自动提交/短码交换，失败可恢复 | 票据用途、签发世代、key version、消费时间唯一约束 | claim 缺失/类型混淆/重放/错节点/日志泄露 | 不透明短码采用不可由输入选择算法的固定 HMAC-SHA256，并按用户/管理员用途分离派生；短码只经浏览器 POST body 传递，Controller/酒馆均设 no-store/no-referrer，数据库仅保存 SHA-256。adapter 以稳定本机密钥调用 loopback Agent 的两个固定核销路由，Agent 先校验 HMAC/时间/nonce，再以当前轮换凭据调用 Controller，酒馆不持有 Controller 凭据。签发把用户 `sub` 固定为 `global_users.uuid`；核销的单条 data-modifying CTE 在写入 `consumed_at` 前同时验证 UUID `jti`、`typ`、精确 `iss/aud/sub`、当前用户和节点账号、目标节点、session/activity epoch、`iat/nbf/exp`、`kid`、Controller generation、活动租约和未撤销/未消费事实，并从当前 `node_accounts` 返回 handle。真实 Router/PostgreSQL 对用户和管理员两类短码逐项篡改全部可变 claim，均得到 403 且 `consumed_at` 保持空；数据库 NOT NULL/FK/typed/check 约束拒绝缺失或类型非法事实，HTTP 矩阵另以缺字段、数字/null/数组、未知字段和尾随 JSON 证明 400 且不消费。交叉用途、错 secret、错节点、32 路并发单次成功、重放、签发响应丢失重试、Controller 重启/凭据轮换及双真实 Agent/adapter 进程路径均已通过。Online 提交 `5ec16f5` 又要求并持久绑定 `(user_id,user_uuid)`，缺失、类型混淆或 UUID 漂移在建立本地 session 前失败关闭；R03 验收闭环，`完成` |
| R04 | 每个用户任意时刻只有一个权威写入节点；A 活跃时选择 B 只能回 A | 事务取得/续约/结束活动租约，条件更新活动世代 | 缓存最后已确认租约并上报，不自行授予新写权 | 会话带 `session_id/activity_epoch/writer_node_id`，逐请求拒绝陈旧写 | 显示当前节点和“请返回 A” | `user_activity_leases` 唯一活动租约、operation ID、epoch | 并发选点、重复请求、网络分区、A/B 双写 | PostgreSQL 条件更新返回数据库计算的精确租约截止时间；Controller 只确认与当前 generation/session/epoch 完全一致且观察时间有界的权威活动事实，Agent 原子持久化确认快照并在本地投递失败或进程重启后重试，不能凭缓存授予新租约。Online 以渠道签名的 loopback 确认绑定本地 session，旧 v4 无界租约迁移时 fail closed，并以固定 60 秒时钟偏差余量提前围栏分区写入者。真实 PostgreSQL 上 32 个同时选择 A/B 的请求仍得到唯一 writer；双真实 Agent/adapter 进程进一步证明：A 活跃时选择 B 只回 A，杀死 A Agent 而保留 A adapter/浏览器后仅在已确认 grace 内可写，数据库租约到期前 B 仍路由 A，A 在中央截止前按余量自行围栏，中央租约自然到期后 B 才取得新 epoch；真实 A/B 并发写入得到 A=409 `stale_writer_session`、B=200，精确写入日志没有双写。logout、Agent 重启、签名错误、快照回滚/重放、投递失败恢复和 omission 撤权矩阵均通过，R04 验收闭环，`完成` |
| R05 | 页面打开但闲置时低频保活；约 15 分钟无页面、无请求、无在途写才离线；陈旧页面恢复必须重新登录 | 会话/页面租约参数化并持久化 | 上报会话与请求计数，不用目录 mtime 猜测在线 | 前后台心跳、用户级请求计数、写门状态、陈旧会话响应 | 明确重新登录提示 | 页面会话、最后活动、租约到期 | 多标签页、后台页、休眠恢复、长请求 | Online 将 idle/前后台 heartbeat 参数化；idle 且无 in-flight 的 session 原子写为 ended tombstone 并撤销精确 lease，持久 tombstone 供 Agent 观察，旧 cookie 即使 tombstone GC 后也不能重建；陈旧页面收到 409/受管认证错误后明确提示并经 Controller 重登，短网络故障继续低频重试。Controller 用 `activity.lease_ttl_sec` 统一 handoff、核销和遥测续约并校验 offline grace；只有精确匹配当前 generation/session/epoch 的 adapter fact 可结束、续约或证明离线，directory fallback、错 session/generation、非法/未来时间全部 fail closed。多标签页、前后台、休眠 409、长请求、边界与重启矩阵通过；实际 Router/HMAC/PostgreSQL 证明自定义 120 秒 TTL、fallback/陈旧 tombstone 不结束 replacement、精确 ended 才结束 lease，R05 验收闭环，`完成` |
| R06 | 快照前只冻结单用户、拒绝新写并排空其在途读写，不停整台酒馆 | 创建绑定租约世代的持久工作流 | 发起写门、等待排空、超时取消并保证最终开门 | 用户级写门及 in-flight 计数覆盖核心/扩展请求 | 展示短暂等待和可重试状态 | workflow/steps、门状态和清理结果 | 请求排空、超时、进程重启、用户回归 | Online 的持久单用户 gate 覆盖实际核心与扩展私有 GET/POST，请求计数按用户排空且不阻塞其他用户；snapshot gate 以精确 workflow/snapshot/epoch/token CAS 续约和释放，并有 10 秒至 5 分钟的有界租期，Agent/adapter 崩溃、续约失败、排空超时都必然自动开门，旧 release 不能删除 replacement 或 data-fault gate。Agent 在复制期间维护租期，失去确认会在截止前取消且绝不进入 transfer；用户回归/管理员取消先在 Agent 原子持久化 job tombstone，再把 backup/workflow/steps/capability/lease 于 PostgreSQL 单事务收敛为 aborted/cancelled/revoked/无租约，重启不能执行排队旧 start 或恢复已取消工作流。Linux 真实进程矩阵以隔离 PostgreSQL、Controller、两个 Agent 和同一持久目录分别杀死 source（gate 建立后）与 target（capability consumed/上传中），证明命令租约恢复、精确 capability 重装备、partial task 清除、唯一 immutable archive 原子发布及最终开门；浏览器扩展在途、错误 token、超时、重启、重复请求与失败边界全部通过，R06 验收闭环，`完成` |
| R07 | 快照工作流为 `scheduled -> quiescing -> drained -> snapshotting -> transferring -> verifying -> publishing -> succeeded`，并支持 `retry_wait/cancelled/failed` | 合法状态机、租约认领、恢复调度 | 本地持久状态、幂等步骤、双端取消与清理 | 冻结/快照内部能力 | 管理员可见阶段与安全错误摘要 | workflows、workflow_steps、operation ID、snapshot ID | 每一分支、重复投递、重启恢复 | PostgreSQL 已实现完整阶段、步骤事实、工作流租约、指数退避、重启扫描和终态；37 个 embedded migration 已在 PostgreSQL 17.10 上由 8 个并发 Store 启动完整应用并校验 checksum。退休协调器只创建绑定 item 的 durable snapshot，由既有 bounded snapshot reconciler 按自身租约续跑；真实 PostgreSQL 已逐阶段发布并重复完成计算/存储迁移工作流。进程验收已证明 Agent 停机期间 queued command 在同一 Agent 状态目录重启后完成；Linux Agent 现覆盖 direct/relay 成功、传输失败和主动取消，并断言源任务目录无论成功或失败都删除。真实 PostgreSQL archive 恢复现从实际 durable backup 产出的 immutable recovery point 开始，由新 Controller worker 经固定命令推进 `scheduled→transferring→verifying→publishing→succeeded`；storage 在目标发布后丢失结果时只读取目标 receipt 收敛，终态重放不再下发命令。R07 补齐了 30 秒短租约与 10 秒续租、每次执行唯一 lease owner、续租丢 fence 即取消、旧执行不得续租或释放新执行租约；真实 PostgreSQL 对 `scheduled/quiescing/drained/snapshotting/transferring/verifying/publishing/retry_wait` 每阶段验证步骤事实、进程失联后的到期重认领、旧 owner 隔离和恢复调度，Linux 双 Agent 进程测试实际杀死并重启 source/target 后完成同一工作流。Agent 进度回执在丢响应时以同一正文和新 nonce 有界重试，Controller 仅接受严格同态重放；snapshot/restore/conflict-resolution 的重试步骤与 capability 撤销在同一串行化事务内重置。管理员 API/UI 显示阶段、attempt、下次重试、cleanup 与安全 reason code/摘要，中止能力完全由后端事实决定，并由真实 PostgreSQL HTTP 验收及 Vitest SSR 覆盖，`完成` |
| R08 | 使用任务临时目录、不可变 snapshot ID、manifest、端到端校验；验证成功后同文件系统原子发布；旧副本后删 | 保存快照与发布事实，拒绝半副本 | 安全打包、限额解包、manifest 重算、原子换代 | 提供受控快照边界 | 显示同步/恢复进度 | snapshot_manifests、replica_copies、发布世代 | 路径穿越、符号链接、炸弹、超大文件、磁盘满、校验失败 | Linux Agent 已实现任务目录、不可变 ID、manifest-first tar.zst、归档/逐文件 SHA-256、路径/类型/数量/大小/窗口/展开比/磁盘余量门禁；真实源→目标 Agent 测试证明旧副本只在完整验证后被替换，失败保留旧事实并清理 task。archive 发布写入私有原 manifest/回执，恢复前重新逐文件 hash，再以新的 restore snapshot/manifest 替换计算数据且不把 archive 元数据带入用户目录。跨平台路径校验现不依赖接收 OS，统一拒绝盘符、UNC、反斜杠、NTFS ADS、NUL、穿越及私有元数据名。R08 补齐每个解包文件 `fsync`、完整 staging 文件/目录树落盘、Linux `renameat2(RENAME_EXCHANGE)` 单原子交换、发布两侧父目录落盘和“新副本 durable 后才删旧副本”；不支持原子 exchange 的文件系统在移动旧副本前失败关闭，绝不降级为会暴露空窗口的两次 rename。Linux 子进程在 `staging_durable/after_atomic_swap/published_durable` 三个精确边界被 `SIGKILL`，新 Agent 在重新开放已消费 capability 前先清理交换遗留，始终保留完整旧副本或完整新副本；内核 `/dev/full` 注入真实 `ENOSPC` 并只返回脱敏 storage-exhausted 原因，32 MiB 不可压缩真实归档完成流式校验/发布且无半副本，`完成` |
| R09 | 默认活动计算副本 + 一个纯存储副本；只保留最新成功副本，不提供 PITR/误删回退 | 角色/配额调度、高低水位、冷却期 | 上报实际可用字节和分配配额 | 无额外核心改造 | 显示保护状态，不承诺历史恢复 | 副本来源、保留原因、保护级别、清理条件 | 存储满、迁移失败、旧副本保留、稳定后清理 | Controller 持久投影纯存储已保护、仅计算临时保护和未保护等状态，并按宽限期去重告警。没有健康 archive 时，调度器在用户离线且 home ready 后按单用户创建 storage repair 快照。深检确定损坏会同时把规范化/遗留 archive 标 `corrupt` 并发 critical alert，使该用户重新进入既有 storage repair 候选；轻检异常保持旧副本 ready 直至 deep 定案。存储退休也固定从当前健康 home 重建到另一台 pure-storage。旧物理目录清理、稳定后的计算临时副本清理及更多故障注入仍缺，`部分` |
| R10 | 直连 Agent-to-Agent 优先，无法直连才用受控、加密、短期中转；控制面与数据面隔离 | 下发每任务短期传输授权和路径选择 | 主动控制连接；数据面用任务能力凭证/TLS | 仅本机内部接口 | 管理员查看路径和进度，不暴露密钥 | Agent 连接、命令 ACK、任务 capability、nonce 消费 | NAT、断线重连、重放、过期授权、中转清理 | 控制面为 Agent 主动长轮询；Agent 监听只挂载无拓扑 health 和 capability-scoped snapshot 数据面，旧入站控制路由不存在。数据面以最多 4 路并发和严格元数据门禁接收，优先用 hash-only、短期、作用域绑定、持久单次消费 capability 直传；直连明确不可达才切换 durable relay。新增真实 Linux source→relay→target 闭环：目标临时 X25519 私钥只在本地持久化，relay 只观察不含用户明文的不透明 ciphertext；目标校验 ciphertext hash、AEAD 上下文、plaintext/archive/manifest hash 后原子发布并确认删除，完成后同 capability 重放在再次下载前失败。所有直接暴露监听固定 TLS 1.3，明文只允许 loopback；真实 NAT/受信证书跨机 TLS 与 relay 断点续传故障矩阵仍缺，`部分` |
| R11 | Agent 一次性短期 enrollment 后自动接入；高权限但仅固定能力、路径 allowlist、可取消、可审计，绝不任意 shell | 绑定角色/任务/指纹原子消费 token；轮换凭证 | 主动出站、固定命令、路径根校验、本地审计 | loopback/Unix socket 能力 API | 一行安装及清晰诊断 | enrollment、credential version、revocation、capability | token 重放/错角色/路径越权/任意命令 | enrollment token 仅存 SHA-256、15 分钟、绑定预建节点/角色/可选指纹并在 serializable 事务单次消费；凭据加密存储且带版本/世代。真实进程验收已分别用两个节点绑定的一次性 token 运行 `agent --register`，核对每个 Agent 配置与临时酒馆 `config.yaml` 原子挂载 node/本地 adapter PSK/generation/loopback Agent URL，随后由该身份完成真实命令；测试不向 fixture 注入旁路凭据。Agent 持久保存最高世代、worker ID、最近 1000 个命令结果和传输 capability 状态；固定命令拒绝未知类型，本地 adapter 代理也只暴露两个固定核销路由并拒绝 nonce 重放，systemd 默认 loopback 和 0077 umask。凭据轮换已有 pending/confirm/activate 持久闭环；新增实际 Router+真实 PostgreSQL 的 Agent HTTP 验收，证明错角色/错指纹不消费、正确 enrollment 单次消费、token 只存 SHA-256，并由该身份完整执行 heartbeat、轮换、lease/ACK/result 固定队列。Agent 自身的实际主动 command channel 现也由 HTTP lease→ACK→execute→result 贯穿，证明 generation header 回滚失败关闭、固定结果落盘后重复 delivery 不重新执行、local audit 标记 cache reuse 且不含 PSK；22 个固定能力的畸形输入逐一返回机器错误，未知 `arbitrary_shell` 只能得到 `unsupported_command`。客户端证书身份、完整本地审计查询和撤销跨进程演练仍缺，`部分` |
| R12 | 单活动主控 + 可选被动副控；单调 `controller_generation`，更高世代才接管，恢复后对账/轮换/撤票 | 控制世代事务和 rebuild 状态 | 持久保存最高世代并拒绝旧命令 | 节点模式与票据世代检查 | 管理员接管/重建状态 | controller_epochs、恢复锁、key version | 双主、旧命令、数据库恢复、凭证轮换 | PostgreSQL session 领导锁保证只运行一个活动控制面；现在每次进程取得领导锁都原子提升 generation，而不是让恢复主控复用副控世代。promotion 同事务撤销浏览器会话/未用票据、废弃旧 pending rotation，并创建逐节点 `controller_rebuild_operations/nodes`；新登录、选点、注册和备份保持关闭，直到所有已接入 Agent 完成旧凭据签名心跳、新凭据轮换及 managed/independent-draining 对账。旧凭据在 middleware 只可进入 recovery heartbeat，Store 命令租约另要求节点已激活当前世代；重复崩溃会把旧 rebuild 标为 superseded，并允许“凭据仍为 N、Agent 已见 N+1、当前为 N+2”的状态继续收敛。真实 PostgreSQL 已覆盖连续两次 promotion、旧凭据不能领新命令、轮换原子废弃旧命令、rebuild 完成才开门；管理员仪表盘显示逐节点进度。二进制级故障测试现以两个真实 Agent 杀死并重启 Controller，验证 generation 单调增加、两个旧凭据只经 recovery heartbeat 收敛、两节点 rotation 激活、逐节点 rebuild succeeded/控制面重开；之后真实 adapter 的票据核销仅经 Agent 当前凭据成功，稳定本机 adapter key 不随 Controller 世代泄漏或失效。实际 HMAC HTTP 验收还证明 pending successor 只能调用 confirm、版本错配失败、激活后旧 PSK 立即 401、current PSK 才能完成 generation/worker fenced 命令。Round 60 灾备链路 2026-08-15 第二批补上真实 pg_dump 端到端（9cf3cc7）：真实库执行器闭环修复 RETURNING ::text 与 OperationID 两个生产缺陷后，生产执行器+真实 pg_dump.exe+HTTP 传输+durable 命令全链验证归档内容与 capability；仍缺被动双主竞争、PostgreSQL 备份还原演练、配置丢失和主密钥恢复演练，`部分` |
| R13 | 短失联维持合法会话但暂停新登录/选点/备份；确认长期失联进入独立模式；恢复为 `independent-draining` | 失联状态机与新操作门禁 | 多信号确认、最后活动归属、有限同伴协调 | managed/independent/draining 状态机 | 简明灾难提示和接管确认 | 模式世代、证据、用户接管事件 | 抖动、分区、长期失联、drain、双故障 | Agent 三信号状态机、命令/注册/备份门禁和 durable 模式世代已实现；真实 adapter 已执行 managed/independent/independent-draining 入口守卫，灾难 session 与待同步用户成为排空事实，重启保持模式。独立模式现在必须同时满足签名 heartbeat 失败、本机公共 health 失败及外部同伴 Agent 对同一 Controller 指纹的 HMAC 见证法定数；名单为空、同伴不可达、合法同伴报告 Controller 可达或重复 node identity 均失败关闭。固定见证端点限制 HTTPS（loopback 除外）、8 个同伴、4 KiB、并发槽、无 query、时间窗、单次 nonce、请求/响应双向 HMAC，不能被用作任意 URL 探测器；共享密钥只从环境变量读取。三信号的持续窗口和各计数原子写入 runtime state、节点列及事件 JSON；任一 health 成功或同伴分歧都会清零 confirmed window，已经开启的 independent 也会立即收紧到 draining，不再签发新灾难登录；旧版缺少见证证据的 independent state 升级后同样自动收紧。Controller ticket 核销后把 `(controller_generation, activity_epoch, owner_node_id)` 固化为最后活动 claim，并经同伴双向 HMAC 法定数复制；查询只接受相同最大不可变 claim 的多数，相同世代/epoch/接管序号却不同 owner 会失败关闭。原 owner Agent/adapter 可用时只能回原节点；最后 adapter 失效时，酒馆先验证真实密码，再展示固定风险确认，短期一次性 challenge 只存 SHA-256；确认后 Agent 对精确 parent claim 执行 durable、可重试、跨重启的多数 CAS，无法取得法定数绝不开放。接管本地审计不记录 handle，成功事实随 heartbeat 幂等写入 `independent_activity_takeovers`，相同 operation 的字段漂移使整个事务回滚。恢复后即使 Agent 错报 pending=0，只要数据库仍有未成功/superseded 的用户对账就保持 draining，用户完成后才返回 managed。真实 PostgreSQL 覆盖接管事件首次写入、重放和篡改冲突，Online 真实登录路由覆盖错密码不查询、明文 challenge 不落盘、错误/重复确认不调用接管。双真实 Agent/双真实 adapter 全进程矩阵现已杀死 Controller 和最后 owner adapter，验证两节点进入 independent、错密码拒绝、一次风险确认、多数 CAS、Controller 新世代恢复、接管 journal 仅在事务提交后 ACK；非 home 灾难写入不会静默覆盖，而是持久 conflict、冻结全局/legacy 身份与活动租约并让节点保持 draining、控制面保持关闭。单节点独立写回归现进一步由 Controller 真实执行器自动建立 immutable archive，并在 adapter 完成命令首次失败、Controller 重启后按同 marker/同 workflow 续跑至 succeeded；终态失败 workflow 清除后有界重建，无合格 storage 时不伪造成功任务。尚缺三节点下整台 owner Agent 丢失、真实跨机分区/抖动和完整浏览器矩阵，`部分` |
| R14 | 统一身份支持密码/Discord/LinuxDo，最多绑定三种且至少一种，无邮箱；密码同步兼容 scrypt hash/salt，不存可逆明文 | 身份表、绑定/解绑、管理员恢复、版本化改密 saga | 精确读写兼容密码材料 | 内部账号供应/查询/改密；受控模式禁原生改密 | 三种登录、绑定管理、无邮箱文案 | auth_identities、node_accounts、password material version | 三种注册登录、绑定上限、部分改密失败 | Controller 的三身份约束、版本化改密和恢复 saga 保持不变；真实 adapter 已挂载幂等 provision/restore/password material 能力，复用酒馆 scrypt hash/salt 且受控模式禁原生改密。计算节点退休若目标账号不存在，会先持久化 workflow-scoped pending account，只下发现有兼容 hash/salt 或 OAuth subject，并按精确 account version/本地用户 ID 回执激活；真实 PostgreSQL 覆盖 pending→active 与重复安全事实。archive 恢复的 Controller 级真实数据库闭环现额外证明：目标无账号时只从既有节点读取版本化 hash/salt，经固定 `restore_user_account` 命令供应并以返回的 local user ID 激活；完成后旧节点账号原子转 stale、目标成为 active，未存可逆密码。新增实际 Router+真实 PostgreSQL 的 LinuxDo/Discord 流程：单次 state、外部 provider 失败、pending enrollment claim/丢响应重放、既有用户登录、session-bound Discord 绑定与公会校验、解绑、离线节点下密码 fallback、至少保留一种身份及密码重新登录均通过；绑定事实同步进 node account 且审计持久。实际改密 HTTP 现证明全局 verifier 与两节点相同版本的 scrypt hash/salt 先原子固化：在线节点即时成功、离线节点恢复后一次命令失败标 error，再由新 Controller worker 使用完全相同材料重试激活；旧密码在节点同步未完成前已失效，命令与审计均无明文密码。OAuth 本地身份的真实在线 Agent 增删 reconciliation 和真实部分命令失败仍缺，`部分` |
| R15 | 注册由用户选择合格节点；邀请码策略属于节点，未知/过期/读取失败时禁止注册；不确定结果幂等查询/重试 | 注册 saga，不静默换节点 | 上报策略版本并幂等供应账号 | 内部供应接口复用真实校验且 operation ID 单次消费 | 重新选节点、pending/确认中/冲突状态 | registration workflows、policy version、node account mapping | 响应丢失、重复请求、邀请码只消费一次 | durable registration workflow 和 fail-closed 节点策略保持不变；adapter 的邀请码 claim 先于账号创建并绑定稳定 `registration_id`，同 claim/同用户可在进程丢失回执后续跑，不同 claim 重用失败，实际 Express 路由测试覆盖单次核销、重复请求和冲突账号。创建 workflow 在事务内同时要求 online/connectivity/active/compatible、reported+desired managed 和 operator allow flag。真实 PostgreSQL 的 Controller→Agent→真实酒馆 adapter 注册现已通过：首个响应按丢失处理，新浏览器以完全相同 operation 续跑至成功，数据库仍只有一个 workflow/用户；同 operation 改正文返回 409。新增实际 Router/PostgreSQL 验收进一步固定跨站来源拒绝、邀请码缺失拒绝、pending cookie 仅存 SHA-256、邀请码/密码材料加密或单向存储、当前世代加密 `provision_user` command、响应丢失轮询、会话签发、原子发布及精确重放/正文漂移冲突。仍缺节点在供应中途宕机和邀请码策略读取失败的同等级双进程注入，`部分` |
| R16 | 老节点自动扫描账号库；OAuth stable subject 自动关联；同名密码账号必须证明控制权；UUID 是全局主键 | 导入批次、歧义状态、认领/合并工作流 | 扫描 `_storage` 账号、OAuth、管理员与目录摘要 | 安全内部扫描接口 | 冲突/认领/合并 UI | node_accounts、import_batches、identity conflicts | 多节点同名、OAuth 关联、不可覆盖旧账号 | durable import facts、OAuth HMAC 自动关联与同名 `claim_required` 语义保持不变；签名 adapter 真实扫描 `_storage` 账号、OAuth subject、管理员和抗符号链接目录摘要。库存以 HMAC revision 固定账号事实，Agent/Controller 校验游标、总数、严格顺序、重复项和跨页来源；变更会失败并用新扫描 attempt 重试，完成批次按原 operation 原样重放。620 账号真实 adapter 路由、10,000 条纯状态机和真实 PostgreSQL 落库/末页读取均通过。新增实际 Router/PostgreSQL 验收由管理员扫描六类账号并分别得到 `already_managed/claim_required/auto_linked/identity_conflict/oauth_unmatched/recovery_required`，证明 node-keyed OAuth 指纹自动关联且 raw subject 不外发；用户随后经加密 `verify_local_user` command 完成同名密码控制权证明，错误密码不认领、成功事务只建立一个 UUID 映射，响应丢失按原 operation 直接读 durable 结果且换节点重放冲突。候选合并的完整浏览器 UI 和多节点同名并发认领仍缺，`部分` |
| R17 | 多个同级总控管理员；节点原生管理员逐节点可撤销关联；短期管理员票据进入后台，不保存明文密码 | 持久管理员会话、关联验证、撤销和审计 | 校验节点当前管理员权限 | 管理员用途专用票据 | 管理员/节点关联状态与跳转 | admins、admin_node_links、admin tickets | 权限降级、撤销、票据类型混淆 | 多管理员、逐节点关联、撤销与用途隔离短码事实保持不变；adapter 已挂载管理员密码验证、每次权限版本复核、专用短码核销和隔离的本地管理员 session。新增实际 Router/PostgreSQL 闭环：管理员 cookie/CSRF 只经当前世代加密固定命令验证节点原生密码，精确 operation 重放读取同一结果；进入后台前再次复核 local user/permission version，专用短码错节点不消费、正确节点单次核销；权限降级将关联标 stale 并原子撤销尚未消费的全部节点管理员票据，显式撤销后不能再签发。该验收同时发现并修复 audit `jsonb_build_object` 未给动态 handle/version/expiry 参数显式类型导致真实数据库失败的两处问题；多管理员并发修改和票据类型混淆的真实浏览器矩阵仍缺，`部分` |
| R18 | 健康分离连通性、角色、容量、兼容、用户副本、运营状态；约 50% 降权，持续 60% 硬门槛并有滞回/冷却/磁盘水位 | 窗口指标、准入原因、可解释推荐 | 上报窗口/峰值/字节/配额/在线数/队列/延迟/版本指纹 | 能力/版本/插件指纹 | 用户只看开放/繁忙/满载/维护/备份/故障 | metrics windows、policy version、compatibility | 波动、滞回、磁盘余量、容量压测 | 持久窗口、50/60% 滞回、磁盘/配额/兼容门禁保持不变；真实 adapter 现在提供页面/请求/in-flight 会话遥测及由酒馆版本、固定能力、插件目录生成的兼容指纹，Agent E2E 验证缺失或伪造 secret 失败关闭。已有兼容事实的节点离线重连或指纹变化会进入绑定 active controller generation 的 durable incident，节点保持 `unknown/upgrade_verifying`，不接受一次成功报告直接重开；只有同一指纹三次严格递增且跨 30 秒的兼容心跳才恢复，等时重试为 no-op、旧报告拒绝，首次接入不误隔离。真实 PostgreSQL 现以 1,000 条 active activity lease 驱动四位数在线事实，验证 50% busy、60% 持续 full、3 分钟冷却/2 分钟恢复、在线数/队列立即硬门、无指标 unknown、等时 no-op、旧心跳拒绝及 metric retention；`CreateRegistrationWorkflow` 也在串行化事务中复核 `open/busy`，杜绝绕过列表直接向 full/unknown 节点注册。客户端延迟持久化已实现（迁移 0044 + RecordNodeClientLatency EWMA + POST /api/users/me/node-latency，Nodes/Register/SelectNode 实测后 fire-and-forget 上报，availableNodeRank 权重同级内按延迟档位次级排序）；更多地区因素的排序与真实并发 HTTP 心跳压测仍缺，`部分` |
| R19 | 计算故障、存储故障、陈旧副本、真实分叉、恢复、节点退役/升级/损坏均进入明确状态机 | 自动编排、用户确认风险、分层告警 | 检测/隔离/按用户恢复/低频校验 | 单用户冻结/恢复接口 | 故障确认、恢复目标选择、冲突差异 UI | lifecycle、conflicts、alerts、integrity checks | 全故障矩阵和进程重启 | 热备接管、archive 恢复、冲突冻结/加密证据/有限合并/原子解冻和节点退休 durable 状态机保持不变。archive 静默损坏现有持久 light/deep 游标、active-generation/lease fencing、2 路 bounded worker 和按用户 quarantine/repair 语义：light anomaly 原副本仍 ready 并立即升级 deep；只有 deep mismatch 才原子标 corrupt、同步遗留 read model 和 critical alert。真实 PostgreSQL 覆盖 light 不能直接隔离、light→deep、深检成功重排 24h/30d 游标、light 不篡改 deep cursor、deep mismatch 隔离；Agent 覆盖同尺寸篡改。外部管理员继续用 Git/PM2 升级而不授予 Controller 任意 shell；Controller 已以 durable `isolated/verifying/resolved` incident、去重告警和审计观察重连、指纹变化及不兼容，并用稳定心跳自动重开。单用户故障闭环现已贯通：Online 持久 `data_fault` gate 原子绑定 fault/handle/activity epoch；Agent 只执行 compute-only 固定 `freeze_user_data`；Controller 管理员入口先在 serializable 事务中结束租约、撤未用票据、标损权威副本并开 critical alert，再由 active-generation/lease-fenced 两路 worker 建立节点冻结门。故障任务在进程/Controller 世代变化后续跑且换代 operation，冻结确认前后端同时禁止接管/恢复；确认后仅由用户选择热备接管或 archive 恢复，并在同一事务中迁移 home、解除故障和告警。请求摘要精确重放、错误 home、重复 open、单用户隔离、旧世代完成拒绝、无恢复点保持关写及热备接管均已有真实 PostgreSQL 证据；archive 分支现进一步覆盖用户确认的精确 recovery timestamp、missing-target-account 供应、每任务 capability、storage 已完成而结果丢失、target receipt 恢复、新 Controller worker 续跑、immutable source 保留、restore manifest 原子发布、唯一新 home/权威副本切换、审计与重复执行幂等。独立灾难回归的双节点写入现以 immutable marker 映射到同一 global user，精确重放仍只有两条 reconciliation；随后规范化/遗留副本统一标 conflict，保护投影创建唯一 open conflict 并冻结 global/legacy identity 与旧活动租约，空 Agent 报告也不能跳过 durable 冲突直接 managed。Linux Agent 新增真实双证据有限合并：本地 immutable evidence 与远端已验证 conflict input 经 prepare/immutable decision page/publish 固定命令选择 remote、保留 base 原件、同文件系统原子替换用户目录，receipt 重放完全相同，计划/决策/receipt 篡改均失败关闭；该链发现并修复首次发布时把“receipt 尚不存在”包装成非法 receipt、导致所有真实发布必失败的问题。节点退休新增 Controller→真实 PostgreSQL→当前世代加密 durable Agent command 的闭环：计算节点从 draining/retiring 建立每用户 snapshot，运行中只持久 defer、不重复创建，发布后原子迁移唯一 home/权威副本，清空依赖并撤销 Agent 凭据后进入 decommissioned；另覆盖空节点后台续跑、仅账号映射、活动用户等待、无目标阻塞、终态 workflow 清理重选、建 workflow 回滚和终态重放。该链发现并修复 `RetryNodeRetirementItem` 在真实 PostgreSQL 下把 item/operation 的同名 `attempt` 引用写成歧义 SQL、使所有等待/失败路径无法持久重试的问题。用户数据故障的 Controller 执行器现由实际管理员 HTTP 报告触发，并经加密 durable command 完成节点冻结；精确请求重放、节点离线 retry、新 worker 恢复和无恢复点继续关写均有隔离 PostgreSQL 证据。archive integrity 的 Controller worker 也已实走 light/deep 固定命令，摘要错配只升级 deep，deep mismatch 才同步 normalized/legacy 副本为 corrupt 并开 critical alert，claim 后掉线只进入 retry_wait。管理员 UI 仅提供固定机器 reason code、权威节点核对和显式风险确认。旧物理目录清理、storage 退休的 Controller 级双进程链和双 Agent 进程故障矩阵仍缺，`部分` |
| R20 | 所有控制台页面连接真实后端、权限、加载/失败/空状态/分页/筛选/审计/进度并响应式可用 | 分页和权限 API、结构化错误 | 上报真实进度 | 提供可观测状态 | 全页面/按钮逐项接线 | 审计/告警/任务可查询 | 组件、API、E2E、权限、响应式 | 节点管理页已接真实四维健康；用户选点页接入真实保护投影、热备接管和 archive 恢复，恢复面板有风险确认、稳定 operation ID 和阶段进度。独立冲突页面已接真实来源选择与提交并保留分页选择；账号导入候选响应和管理员页面固定每页最多 100 条。管理员节点页的真实退役进度 API 每 15 秒刷新总项/完成/等待离线/阻塞/失败，后台执行器现已接入并把 draining 自动推进为 retiring/decommissioned；同页也从管理员专用 API 刷新兼容 incident 的隔离原因及稳定心跳进度，按钮仍只按后端合法状态显示。管理员用户页新增权威故障报告和持久状态刷新，只接受固定机器原因并要求风险确认；用户保护页的操作可用性仍由后端确定性状态决定。仪表盘新增 Controller rebuild 的 generation、总数和逐节点确定性进度，并把加载失败显式呈现而非静默空白。新增实际 Router+真实 PostgreSQL 的 HTTP 验收，贯穿管理员/用户登录、cookie/CSRF、角色隔离、no-store、用户各只读页、登出撤销、总览/节点/用户/备份/告警/管理员分页与空状态、节点配置/enrollment/lifecycle/retirement/import/link 错误边界、管理员创建/改密/禁用/最后管理员保护和用户禁用后的会话失效；该链发现并修复 `/api/auth/logout` 被已有 chi 子树遮蔽成 404 以及管理员禁用时间参数在真实 PostgreSQL 被推断成 text 两个生产问题。2026-08-15 第二批又以 web/src/adminTypes.ts（27 个 DTO）消除 Admin.tsx 全部 39 处 adminApi any 返回类型（建议 #8，bd28ae6），AI 管理页线格式回归测试固定 snake_case 键（4e2367b）；生产构建通过，但全站筛选/响应式、组件测试与浏览器 E2E 仍未完成，`部分` |
| R21 | 目标容量：注册过万、同时在线四位数、单节点数百并留冗余；控制请求和大文件传输隔离 | 索引、批处理、限流、队列、公平调度 | 并发/带宽/磁盘限额 | 心跳与写门低开销 | 大列表分页/虚拟化 | 关键索引与队列事实表 | 登录/心跳/队列/备份/数据库压测 | 账号库存不再以 500 截断：adapter/Agent durable result 每页最多 250，Controller 最多 40 页且逐页校验 revision/cursor/total/order/source，数据库和 UI 每页最多 100。真实 PostgreSQL 17.10 在 9.2 秒内分类并持久化 10,000 个候选，首末页均受界；另有 620 账号真实 Express 路由和变更中断测试。8 并发启动、32 租约、32 核销也已通过。新增真实 PostgreSQL 四位数在线基线在 10 秒门限内原子建立 1,000 个 legacy/global/account/active-lease 事实（本机实测约 1 秒），再用 100 条上限 cursor 分页无遗漏遍历；两个节点各 32 条交错创建的控制命令按节点隔离并严格 FIFO 租用，一个节点的旧积压不阻塞另一个节点。HTTP 层限流 2026-08-15 修复闭环：移除全局 RealIP 头信任（伪造头不再改写限流 key，clientIP 仅信私网/loopback peer 的 XFF 并归一化 IPv4-mapped IPv6），120/min 全局每 IP 限流真正挂载到 /api 用户与管理路由组（agent HMAC 通道与健康检查除外），20/min 登录限流 + 用户名锁定保持；1k 客户端真实并发登录/HTTP 心跳、备份吞吐和带宽故障压测仍缺，`部分` |
| R22 | 安全和隐私：TLS、目录权限、日志脱敏；不记录聊天/API key/密码/token；恶意归档与权限绕过必测 | 安全头、限流、错误脱敏、审计摘要 | TLS 校验、日志脱敏、安全解包 | no-store/referrer/本地接口认证 | 不泄露内部拓扑/任务 ID | 最小化审计数据和保留策略 | token/nonce/路径/归档/日志扫描 | 既有 nonce、CSRF/cookie、OAuth、Authorization、恶意归档和 0700/0600 边界保持不变；adapter 本地代理只接受 HMAC/时间/单次 nonce，本机 key 与 Controller 轮换 key 分离并限制固定路由/大小，直接监听固定 TLS 1.3。安全归档命名现在使用与宿主 OS 无关的统一 allowlist，Linux 测试证明 Windows drive/UNC/ADS/反斜杠与混合穿越不会进入解包或冲突证据路径；relay 闭环断言中转 ciphertext 不含用户明文，重放在下载前拒绝。实际 Agent Router 验收补充未知 node、错 PSK、pending PSK 越权、旧 PSK、错 worker、结果重放和 byte-identical nonce 重放边界：即使首个签名请求只到达 400，nonce 也已原子消费，第二次必为 401。Controller access logger 现只在克隆请求中把全部 query 替换为 `?redacted`，handler 仍收到原 OAuth code/state；单元测试固定证明两项秘密不进日志。`Audit` 把空 detail 作为 SQL NULL 而非非法空 JSON，并拒绝非 JSON 明细，OAuth 身份链已在真实库核对四条审计落盘。完整性层只返回 hash/count/bytes/check-kind，Store 禁止 light 直接写 corrupt；仍缺全仓日志敏感样本扫描、受信 CA 的真实跨机握手和完整扩展权限绕过矩阵，`部分` |

## 当前基线证据

- 2026-08-15 第二批复验基线：真实 PostgreSQL `go test -count=1 -run 'TestPostgres|TestController' ./internal/store/ ./internal/controller/` 44 PASS / 0 SKIP / 0 FAIL（含决策④采纳 3 例、真实 pg_dump 端到端 1 例、采纳 HTTP 1 例）；48 个迁移（0001–0048）连续；`go build/vet/test -short ./...` 全绿；web tsc + vitest 16/16；Online 5 个 stcontrol 套件 28/28。跨平台合并覆盖率与 80% 门禁差距、-race、跨机矩阵仍按“Linux 服务器迁移后验收”口径保留。

- 设置真实 PostgreSQL DSN 后执行 `go test -count=1 -coverpkg=./... -coverprofile=coverage/real_crosspkg.out ./...`、`go vet ./...`、`go build ./...`：全部通过；Windows 全量按文件区间去重覆盖率 63.9%。同一提交另以 WSL 内项目私有 GOPATH/GOCACHE、官方 Go 1.22.5 执行 Linux `internal/agent` 全套并生成跨包 profile；两个平台按相同文件区间取最大命中后为 66.7%（Agent 72.1%、Controller 63.6%、Store 67.0%），这是包含 Linux-only 安全文件系统路径的当前门禁基线。本轮真实数据库套件、双节点进程验收、Controller direct/relay 备份、archive restore、灾难排空/接管/双写冲突/独立写对账、capacity/queue scale、conflict evidence/resolution/节点退休/用户数据冻结/分层副本校验/密码同步故障矩阵及实际 Router 管理/用户/Agent/OAuth/注册/导入/票据 HTTP 验收均包含在全量测试中；总覆盖率仍远低于最终 80% 门禁，不能据此宣称上线完成。Windows `-race` 因宿主 C 编译器不支持 64-bit `runtime/cgo` 而无法构建，WSL 也未安装 gcc，均未计作通过。
- 本阶段将 R20/R22 继续保持为 `部分`，新增 `internal/controller/admin_http_postgres_integration_test.go`：在隔离 PostgreSQL schema 上通过实际 `Server.Handler()`、HTTP cookie jar 和双提交 CSRF 贯穿公开、用户和管理员路由，核对错误/空状态/分页/筛选/权限/撤销/精确 lifecycle replay/enrollment 仅存 SHA-256/最后管理员保护及响应脱敏。默认单元套件中的 `internal/controller/session_test.go` 另固定验证 `/api/auth/logout` 路由不会再被 `/api/auth` 子树遮蔽，并原子撤销 durable session；该链发现并修复 `internal/controller/router.go` 的登出 404 与 `internal/store/admins.go` 的 `$3::timestamptz` 真实 PostgreSQL 类型错误。尚缺真实浏览器组件/响应式/全页面操作矩阵，因此未误标完成。
- 本阶段将 R11/R12/R22 继续保持为 `部分`，新增 `internal/controller/agent_http_postgres_integration_test.go`：实际 Router 在隔离 PostgreSQL 上拒绝错角色/指纹且不消费 token，正确 enrollment 单次核销并仅存 hash；随后覆盖未知 node/错 PSK、完整 heartbeat、30 天凭据轮换、pending 仅 confirm、版本错配、激活后旧凭据撤销、HMAC nonce 原子重放拒绝、加密 v2 command envelope/HMAC 复算、错 worker ACK、成功结果和结果重放。全量真实数据库跨包覆盖率由 55.2% 提升到 56.7%；仍缺 mTLS 客户端证书、跨机撤销演练和全仓敏感日志扫描，故未误标完成。
- 本阶段将 R14/R22 继续保持为 `部分`，新增 `internal/controller/oauth_http_postgres_integration_test.go` 与 query-redacting access logger：真实 Router/PostgreSQL 贯穿 LinuxDo 新用户/既有用户、pending 丢响应重放、Discord session-bound 绑定/公会校验、身份解绑、密码 fallback 和最后身份保护；外部 provider 仅由受控 RoundTripper 替换，Controller/Store/会话/CSRF 均为生产路径。测试发现并修复 `BindOAuthIdentity` 的 `jsonb_build_object` 参数无类型导致真实绑定 500，以及 nil `Audit` detail 被 lib/pq 当空 JSON 导致所有无明细审计静默失败；默认单元测试证明 OAuth code/state 不进 access log、空审计为 SQL NULL、坏 JSON 不落库。覆盖率由 56.7% 提升至 58.0%，尚缺在线 Agent 的 OAuth 增删部分失败 reconciliation，故未误标完成。
- 本阶段将 R15/R16/R22 继续保持为 `部分`，新增 `internal/controller/registration_http_postgres_integration_test.go` 与 `internal/controller/import_http_postgres_integration_test.go`：实际 Router、cookie/CSRF、隔离 PostgreSQL 和当前世代加密 Agent command 贯穿邀请码注册、pending 丢响应恢复、精确重放，以及六类旧账号扫描、OAuth 指纹关联、错误/正确密码控制权证明和 claim 丢响应重放；数据库断言 bearer/邀请码/密码/raw OAuth subject 均不以明文进入持久事实。该链发现并修复 lib/pq 不允许一个 prepared statement 执行三条 claim 发布 SQL 的生产错误，并把三次更新拆为同一 serializable transaction 内各自核对恰好一行；同时补上成功认领后原 operation HTTP 重试此前因候选已离开 `claim_required` 而错误返回 409 的 durable replay 缺口。覆盖率由 58.0% 提升至 59.5%，仍缺供应中途双进程宕机、策略读取故障和候选合并浏览器 UI，故未误标完成。
- 本阶段将 R03/R17/R22 继续保持为 `部分`，新增 `internal/controller/handoff_http_postgres_integration_test.go`：实际用户/管理员登录、cookie/CSRF、当前世代加密管理员校验命令、HMAC Agent 身份与真实 PostgreSQL 贯穿两类短码签发/签发重放/错节点/原子核销/核销重放，并验证管理员权限版本复核、降级 stale、未消费票据撤销和显式解除关联；command payload 与票据表均扫描确认没有明文节点密码或 bearer code。该链发现并修复 `admin_node_links`/`admin_handoffs` 审计 JSON 参数在 lib/pq 下无类型、以及用户 handoff 响应丢失重试因随机 session ID 与原 operation 冲突两个生产问题。Windows 全量覆盖率由 59.5% 提升至 60.5%，重跑 Linux Agent 后跨平台合并基线为 62.4%；仍缺全部 ticket claim 类型混淆的双进程浏览器矩阵，故未误标完成。
- 本阶段将 R11/R19/R22 继续保持为 `部分`，新增 `internal/agent/commands_channel_test.go` 与 Linux `conflict_resolution_run_linux_test.go`：前者贯穿主动命令通道、generation/header/status 错误、ACK/result、结果持久缓存、本地脱敏审计和 22 类固定能力畸形输入/任意 shell 拒绝；后者用真实文件、两端 manifest/hash 和固定命令贯穿有限合并 prepare→decision page→原子 publish→receipt replay，并验证计划/页面/receipt 不可变。该链修复 `readConflictResolutionReceipt` 丢失 `os.IsNotExist` 语义导致首次真实 publish 永远失败的生产问题。Windows 全量覆盖率由 60.5% 提升至 61.3%，Linux Agent 提升至 72.3%，跨平台合并基线为 64.1%；仍未达到 80% 门禁。
- 本阶段将 R14/R19/R20 继续保持为 `部分`，新增 `internal/controller/node_retirement_postgres_integration_test.go`：隔离 PostgreSQL 上由生产 `executeNodeRetirement` 建立计算节点退休 snapshot，真实 durable command harness 完成账号供应、快照、传输、manifest/hash 原子发布与 home 提升，再自动清空依赖、撤销节点访问并 decommission；重复 coordinator pass 只 defer 已运行 workflow，完成重放不再执行。故障子矩阵覆盖空节点 bounded reconciler、缺 worker 身份、account-only 映射、活动租约等待、无目标、终态 workflow 清除、容量关闭后的重选以及账号材料不足时事务回滚并持久 retry。测试发现并修复 `internal/store/node_retirements.go` 的 `attempt` 歧义 SQL。Windows 全量覆盖率由 61.3% 提升至 62.1%，跨平台合并基线为 64.9%；仍缺 storage 退休 Controller 双进程、浏览器进度矩阵和最终 80% 门禁。
- 本阶段将 R19/R20 继续保持为 `部分`，新增 `internal/controller/user_data_fault_postgres_integration_test.go` 与 `replica_integrity_postgres_integration_test.go`：实际管理员 Router/cookie/CSRF 报告单用户权威数据故障，只经当前世代加密 `freeze_user_data` 固定命令建立节点 gate，响应丢失重放不重复命令；节点离线先持久 retry，新 Controller worker/节点恢复后由 bounded reconciler 续跑。分层 archive 校验同时通过真实 command queue 覆盖 light 成功、light receipt 摘要不符立即 deep、deep 成功、deep mismatch 原子隔离/critical alert，以及 claim 后节点丢失的非破坏 retry。两条链均核对命令 HMAC、generation、不可变摘要、唯一状态与审计。Windows 全量覆盖率由 62.1% 提升至 62.9%，跨平台合并基线为 65.7%；仍缺真实浏览器页面与跨机磁盘故障矩阵，未误标完成。
- 本阶段将 R13/R14 继续保持为 `部分`，新增 `internal/controller/password_sync_postgres_integration_test.go` 与 `independent_reconciliation_postgres_integration_test.go`：实际用户 Router/cookie/CSRF 改密先在一个事务内更新全局 verifier 并为两个节点固化同一版 scrypt hash/salt，在线节点立即走加密 `set_password`，离线节点恢复后首次命令失败标 error、新 Controller 再按原材料续跑；旧密码立即失效且命令不含明文。独立模式单写在 `independent-draining`/零 session 后由 Controller 自动建立 immutable archive，再以固定 `complete_independent_sync` 命令完成 adapter marker；首次完成失败进入 durable completion retry，新 worker 收敛，终态 snapshot 会清除 workflow 后有界重建，无 storage 目标不创建假任务。真实库测试发现并修复 completion gate 把 marker 参数误用作时间比较，以及 completion/snapshot retry 的 CASE 把 timestamptz 推断为 text 三处生产 SQL 问题。Windows 全量覆盖率由 62.9% 提升至 63.9%，跨平台合并基线为 66.7%；最终 80% 门禁仍未达到。
- `npm run build`：通过，只证明 TypeScript/Vite 能构建，不证明页面行为、权限、错误状态或后端接线。
- `STCONTROL_TEST_POSTGRES_DSN=... go test -count=1 -run '^TestPostgresCriticalConcurrency$' -v ./internal/store`：通过；37 个 migration 并发应用/checksum 核对，另含连续两次 Controller promotion/rebuild/凭据轮换、旧凭据命令围栏、32 路单写租约、32 路一次性核销、接管/未映射灾难事实、退休执行器闭环、tiered integrity、外部升级兼容 incident、权威数据故障跨 generation 恢复及 10,000 条账号库存。
- 本阶段 R19 冻结执行底座的 stcontrol 代码证据为 `internal/protocol/protocol.go`、`internal/agent/commands.go`、`internal/agent/control_mode.go` 与 `internal/agent/tavern.go`，测试证据为 `internal/agent/data_fault_test.go`、`internal/agent/commands_test.go`；Online 代码证据为 `src/stcontrol.js`、`src/endpoints/stcontrol.js`，测试证据为 `tests/stcontrol-adapter.node.test.mjs`、`tests/stcontrol-routes.node.test.mjs`，对应已推送提交 `18bdc67`、`39e7fab`。
- 本阶段 R19 Controller/PostgreSQL 代码证据为 `internal/store/migrations/0033_user_data_faults.sql`、`internal/store/user_data_faults.go`、`internal/controller/user_data_faults.go`、`internal/store/protection.go`、`internal/store/restores.go` 与 `web/src/pages/Admin.tsx`；测试证据为 `internal/store/user_data_faults_test.go`、`internal/controller/user_data_faults_test.go`、既有 protection/restore 测试及 `internal/store/postgres_integration_test.go` 的 `authoritative_user_data_faults_freeze_one_user_and_resume_across_generation_change` 子测试。
- 本阶段 R12 代码证据为 `internal/store/migrations/0034_controller_rebuilds.sql`、`internal/store/controller_rebuilds.go`、`internal/store/leadership.go`、`internal/store/control_modes.go`、`internal/store/agent_credentials.go`、`internal/controller/middleware.go` 与 `cmd/controller/main.go`；测试证据为 `internal/store/controller_rebuilds_test.go`、`internal/store/control_modes_test.go`、`internal/controller/middleware_recovery_test.go` 及真实 PostgreSQL 的 `controller_promotion_only_permits_heartbeat_recovery_before_credential_rotation` 子测试。
- 本阶段 R03/R04/R11/R12/R15/R22 代码证据为 `internal/agent/adapter_proxy.go`、`internal/agent/tavern_config.go`、`internal/controller/tickets.go`、`internal/store/handoffs.go`、`internal/store/leases.go` 与 Online 提交 `5d92ce2` 的 `src/endpoints/stcontrol.js`；单元证据为 `internal/agent/server_test.go`、`internal/agent/tavern_config_test.go`、`internal/store/handoffs_test.go` 和 Online 的 `tests/stcontrol-routes.node.test.mjs`。
- 本阶段 R06/R07/R08/R10/R19/R22 代码证据为 `internal/agent/snapshot.go`，Linux 故障矩阵为 `internal/agent/snapshot_run_linux_test.go`，跨平台恶意路径矩阵为 `internal/agent/snapshot_test.go`。校验官方 SHA-256 的 Go 1.22.5 Linux 工具链在 WSL 原生文件系统上执行完整 `internal/agent` 套件通过：Agent 覆盖率 63.4%，其中 `RunSnapshot` 73.8%、`RunRestoreTransfer` 66.7%、`RunRelayReceive` 76.3%；总仓 80% 门禁仍未达到。
- 本阶段将 R07/R08/R09/R11/R12 继续保持为 `部分`，新增 Controller 真实数据库证据 `internal/controller/backup_postgres_integration_test.go`：隔离 PostgreSQL schema 中的 source/target 执行器只走当前世代的加密 durable command lease/ACK/result，逐条解密 v2 envelope 并复算 Agent PSK 派生 HMAC；覆盖正常原子发布与终态重放、目标 prepare 失败后的 `retry_wait`/新 Controller worker 续跑、目标已经 `publishing` 但源命令结果丢失后的 receipt 恢复，同时核对 command 历史、immutable manifest、唯一 ready archive、单次 capability 消费和无重复发布。尚缺双端真实进程中间阶段崩溃、掉电/磁盘满及被动双主等矩阵，因此未误标完成。
- 本阶段将 R07/R10/R11/R12 继续保持为 `部分`，同一真实数据库测试新增两条 encrypted relay 编排路径：直连仅在 Agent 返回固定 `snapshot_direct_unreachable` 后一次性持久切换 `transfer_mode=relay`，由新 Controller worker 续跑；source/target 仍只经 durable 固定命令，relay bearer 仅以 32-byte hash 入库，`relay_transfers` 完整经过 prepared/uploading/stored/downloading/consumed。正常路径核对两端 receipt 完全一致；源端在目标 publishing 后丢失结果时改从目标读取 durable receipt，仍只发布一个 immutable archive。尚缺真实 NAT/受信 CA 跨机进程与 relay 断点续传矩阵，因此未误标完成。
- 本阶段将 R19/R22 继续保持为 `部分`，新增真实 PostgreSQL 证据 `internal/controller/conflict_postgres_integration_test.go`：两个 compute 副本从真实保护投影触发 conflict，事务冻结 global/legacy identity 与活动租约；Controller 仅经当前世代加密 durable command 捕获每端 101 条有序 manifest，按 60 条形成两页 response ciphertext，再以逐 evidence 派生 key 重加密落库并把命令结果脱敏；用户确认固定 base 和 101 条 `preserve_both` 后，选择按 100+1 两个 command page 下发，非 base 传输的丢失回执从 base 恢复，新 Controller worker 最终原子发布 immutable result、唯一权威 home、旧来源 stale、身份/租约解冻及审计。该测试发现并修复 `evidence_id` 漏写、JSON 审计参数缺 cast、写不存在 users 列和 `primary/home` 错配四个真实 schema 漂移。尚缺双 Agent 进程中途崩溃、超大证据和旧物理目录清理，故未误标完成。
- 本阶段将 R07/R14/R19 继续保持为 `部分`，新增真实 PostgreSQL 证据 `internal/controller/restore_postgres_integration_test.go`：先经现有加密 durable command 路径生成真实 immutable archive，再由 HTTP 用户确认入口以精确 recovery timestamp 创建恢复；目标账号不存在时只下发版本化 hash/salt 的固定供应命令。storage 发起恢复后把目标推进到 publishing 并模拟响应丢失，新 Controller worker 读取 target receipt 后在一个事务内消费 capability、固化 restore manifest/hash、把旧 home/account 标 stale、切换唯一新 home/权威 copy，并保留原 archive immutable；终态重放不新增命令，scheduled/succeeded 审计各一条。真实数据库还暴露并修复了 restore 两处 `jsonb_build_object` 参数未显式定型的问题。
- 本阶段将 R13/R19 继续保持为 `部分`，代码证据为 `internal/agent/control_mode.go`、`internal/store/control_modes.go`、`internal/store/independent_reconciliations.go` 和迁移 `0035_confirmed_controller_outage.sql`；测试证据为 `internal/agent/control_mode_test.go`、`internal/store/control_modes_test.go` 与真实 PostgreSQL 的 `internal/controller/control_mode_postgres_integration_test.go`。本轮先发现并修复 heartbeat-only 起点被错误计入双信号持续时间、空 Agent 报告可绕过数据库 pending reconciliation，以及 independent conflict SQL 带多余参数三个安全/真实 schema 问题；随后覆盖 Agent 重启、单用户自然排空、marker 丢失、终态 managed、A/B 独立双写锁存/冻结、报告重放和旧 mode generation 拒绝。
- 本阶段继续保持 R13 为 `部分`：`internal/agent/peer_witness.go` 新增固定只读同伴见证端点与客户端法定数，`internal/agent/control_mode.go` 将自动 independent 收紧为 heartbeat/本机 health/外部见证三信号持续门槛，`internal/store/migrations/0036_peer_witness_evidence.sql`、`internal/store/control_modes.go` 和 `internal/controller/agent.go` 持久化并复核见证证据。`internal/agent/peer_witness_test.go` 覆盖双向 HMAC、nonce 重放、错 Controller 指纹、同节点、query/超限、TLS/名单上限、健康分歧、同伴不可达、共享故障、重复节点身份和陈旧响应；`internal/agent/control_mode_test.go` 覆盖分歧清窗、持续法定数、重启和旧 runtime state 收紧；真实 PostgreSQL 的 `TestControllerDisasterDrainAndMultiNodeWriteConflict` 在 0036 migration 上通过。
- 本阶段 R13 跨 adapter 最后活动归属代码证据为 `internal/agent/activity_ownership.go`、`internal/agent/adapter_proxy.go`、`internal/agent/control_mode.go`、`internal/store/independent_takeovers.go`、迁移 `0037_independent_activity_takeovers.sql` 与 Online 提交 `9ad4430`。Go 测试 `internal/agent/activity_ownership_test.go` 覆盖两节点唯一 claim、同 epoch 分叉、owner 可用/adapter 失效、法定数 CAS、无 quorum、peer nonce 重放、重启及精确 operation 重放；`internal/controller/control_plane_test.go` 覆盖边界归一化，真实 PostgreSQL `confirmed_disaster_takeover_is_immutable_and_idempotent` 覆盖首次写入、幂等重放和 operation 字段篡改回滚；Online `tests/stcontrol-disaster-login.node.test.mjs` 走实际 `/api/users/login` 覆盖密码前置、只存 challenge hash、显式确认、Agent 成功和一次性 challenge 重放。完整双酒馆二进制故障链仍缺，故未误标完成。
- 本阶段 R18/R21 代码证据为 `internal/store/nodes.go`、`internal/store/node_capacity.go`、`internal/store/registrations.go`、`internal/store/admin_pagination.go` 和 `internal/store/commands.go`；测试证据为 `internal/store/node_capacity_postgres_integration_test.go` 与 `internal/store/registrations_test.go`。隔离真实 PostgreSQL schema 中建立 1,000 个在线租约，验证窗口/滞回/冷却/磁盘与计数硬门、full 注册事务硬拒绝、心跳重放/过期、100 条 cursor 分页、metric retention，以及两节点各 32 条交错控制队列的节点隔离/FIFO；真实 HTTP 并发登录/心跳和数据面吞吐尚未完成，因此 R18/R21 仍为 `部分`。
- 本阶段进程验收证据为 `internal/agent/process_e2e_test.go` 与 Online 提交 `5d92ce2` 的 `tests/fixtures/stcontrol-adapter-server.mjs`：隔离 PostgreSQL schema 上真实编译/启动 Controller、两个 Agent、两个真实 adapter；两个一次性 enrollment 原子挂载临时酒馆配置；管理员 cookie/CSRF 发起 durable scan；Agent 停机排队后重启续跑；Controller 杀进程后提升 generation、轮换两节点凭据、rebuild 开门；随后完成响应丢失注册恢复、冲突重放、A writer、错 B 核销、正确 A 核销、点选 B 返回 A 及一次性重放拒绝。该测试仅在设置 `STCONTROL_TEST_POSTGRES_DSN` 时运行，未设置时显式 skip，避免把无数据库的绿色结果计为验收。
- 本阶段继续扩展同一真实进程验收，Online 提交 `1e90b4a` 让 fixture 挂载实际 `/api/users/login`、`/logout` 并共享会话；`internal/agent/process_e2e_test.go` 在受管 handoff 复制最后 owner claim 后杀死 Controller 与 owner adapter，验证双 Agent 三信号进入 independent、错密码 403、正确密码先返回固定 423 风险确认、一次性 challenge 经多数 CAS 后登录成功、两端 ownership 只前进一次且本地审计不含 handle。随后重启 owner adapter 与 Controller，证明新 generation 下接管事实先原子写入 PostgreSQL、再由响应 ACK 清理 Agent journal；退出灾难 session 后，非 home 写入被 durable reconciliation 标为 conflict，保护投影冻结 global/legacy identity 和旧租约，节点保持 `independent-draining` 且控制面不开门。该链发现并修复 unmapped 告警把 `node_id` 推断成 text 的真实 PostgreSQL 类型错误；`unmapped_independent_user_is_durably_frozen_and_alerted` 子测试进一步证明重放只保留一个 reconciliation/alert 并递增 occurrence。
- WSL Ubuntu-E 使用校验过 SHA-256 的官方 Go 1.26.5 Linux 工具链执行 `TestAgentDataPlaneHandlerPublishesCapabilityOnceEndToEnd`：通过；真实 Linux 原子发布一份 HTTP 快照后，同一 capability 重放被拒绝。
- `node --test tests/stcontrol-disaster-login.node.test.mjs tests/stcontrol-routes.node.test.mjs tests/stcontrol-adapter.node.test.mjs tests/stcontrol-agent-auth.node.test.mjs tests/stcontrol-heartbeat.node.test.mjs`：18/18 通过；除实际 adapter 的 620+ 账号分页外，新增覆盖故障门首次建立/精确重放/范围冲突、进程重启持久化、快照互斥、只读可用/写入 423 `user_data_frozen`，以及灾难接管的密码前置、显式确认和一次性 challenge。
- Online 酒馆三项明确修复已由已推送提交 `316cb65` 与后续格式扩展 `4832be3` 落地，并在当前 `online` 头 `b98803b` 复验：`public/scripts/chat-paging-settings.js`、`public/script.js`、`public/index.html` 与 `public/scripts/power-user.js` 让“聊天/消息处理”的加载条数（1..1000，0=全部）实际决定字符/群聊首批加载，不再被固定 20 条覆盖；`public/scripts/admin-extensions.js`、`public/scripts/templates/admin.html`、`src/free-gemini-channels.js` 与 `src/endpoints/backends/chat-completions.js` 按渠道 ID+模型 ID 持久化 `gemini/openai/openai-responses/anthropic`，逐模型切换模型发现、状态、URL、鉴权、请求体、普通/流式响应转换、重试和故障切换，未配置模型仍默认原生 Gemini；`public/scripts/templates/userReset.html`、`public/scripts/user.js`、`src/account-reset.js` 与 `src/endpoints/users-private.js` 已删除控制台重置码流程，只接受当前登录用户名确认。当前独立复验 Free Gemini 41/41、账号重置 3/3、群聊分页 8/8、加载设置与 non-chunked tail 3/3；覆盖同一渠道 Gemini/DeepSeek/Claude/Responses 普通及流式请求、跨格式重试故障切换、刷新/磁盘持久化、0=全部、225 条群聊页、用户名缺失/错误不删除和正确用户名删除。
- R02 的 Online 提交 `ed6913b` 关闭了此前发现的三个旁路：浏览器宿主 `/login`、`/register` 与 LinuxDo `/oauth` 现在在 handler 前应用 adapter guard；启用 stcontrol 时 `tryAutoLogin` 无条件失败关闭，避免单用户、trusted header 或 Basic 在 independent 中绕过密码后活动归属法定数。`tests/stcontrol-native-entry-guards.node.test.mjs` 动态枚举实际公开/私有/管理员/OAuth Router 的全部 method/path，并验证 managed、controller-unreachable、independent、independent-draining；与既有 adapter/Agent auth/灾难登录/心跳/实际路由套件同跑 23/23 通过，明确证明 independent 只开放显式登录而注册、OAuth、恢复、改密、删号和管理员变更继续返回 423。
- 本阶段以 `internal/agent/scan.go` 的统一目录判定、`internal/agent/commands_test.go` 的库存/活动/容量矩阵和 `internal/agent/snapshot_test.go` 的 manifest→archive→原子发布闭环关闭 R01；Windows 上设置工作区内 `TEMP/TMP/GOTMPDIR/GOCACHE/GOMODCACHE` 并连接真实 PostgreSQL 后执行 `go test ./... -count=1`，全部包通过（Agent 65.285s、Controller 50.090s、Store 19.055s）。
- 本阶段以 `internal/store/handoffs.go`、`internal/store/admin_handoffs.go`、`internal/controller/tickets.go` 和 `internal/controller/admin_node_links.go` 关闭 R03：固定用途分离 HMAC、稳定用户 UUID subject、当前身份/账号/节点/租约与完整时间/key/generation claim 在原子核销前共同校验。`internal/controller/handoff_http_postgres_integration_test.go` 通过实际 Router、Agent HMAC 与隔离 PostgreSQL 执行两类票据的畸形 JSON、逐 claim 篡改、交叉用途、错节点、有效核销和重放矩阵；`internal/store/postgres_integration_test.go` 保持 32 路并发只成功一次。Online 提交 `5ec16f5` 在真实 adapter 路由中拒绝缺失/错误类型/漂移 UUID，并把稳定 UUID 与数值用户 ID 一起绑定本地账号；六个 stcontrol Online 套件 23/23 通过。Windows 连接真实 PostgreSQL 的 `go test ./... -count=1`、`go vet ./...`、`go build ./...` 全部通过。
- 本阶段以 Online 提交 `0c8c7fb` 的 `src/stcontrol.js`、`src/endpoints/users-public.js`、`public/scripts/user-heartbeat.js` 及 adapter/heartbeat 测试关闭浏览器侧 R05；当前六个 stcontrol Online 套件 28/28 通过。Controller 侧证据为 `internal/controller/agent.go`、`internal/controller/tickets.go`、`internal/store/leases.go` 与 `activity.lease_ttl_sec` 配置；`activity_test.go` 覆盖前后台、请求、in-flight、idle/ended、未来时间、fallback 与空权威快照，实际 Router/HMAC/PostgreSQL 测试覆盖 120 秒自定义 TTL、陈旧/精确 tombstone 及 handoff 签发核销。连接真实 PostgreSQL 的 `go test ./... -count=1` 全部通过（Agent 60.868s、Controller 49.004s、Store 17.000s），`go vet ./...`、`go build ./...` 通过。
- 本阶段以 Online 提交 `b98803b` 和 stcontrol 的 `internal/agent/activity_leases.go`、`internal/controller/agent.go`、`internal/store/leases.go`、`internal/store/handoffs.go` 关闭 R04：Controller 返回 PostgreSQL 精确截止时间与因果观察时间，Agent 对确认快照做有界校验、原子持久化、回滚拒绝和重启重投，adapter 只接收签名固定路由并逐请求围栏。`internal/agent/process_e2e_test.go` 在真实 PostgreSQL 上启动 Controller、两个 Agent 和两个真实 adapter，杀死 A Agent 保留 A 浏览器/adapter，覆盖 grace、提前围栏、自然到期、B 接管、A/B 同时写（A=409、B=200）、精确写日志、logout 与 Agent 重启；该进程测试 194.85 秒通过。Online 六个 stcontrol 套件 29/29 通过；连接真实 PostgreSQL 的 `go test ./... -count=1` 全部通过（Agent 225.395s、Controller 50.597s、Store 19.579s），`go vet ./...`、`go build ./cmd/controller ./cmd/agent` 通过。
- 本阶段以 Online 提交 `f5674b5` 的 `src/stcontrol.js`、`src/endpoints/stcontrol.js` 及 adapter/routes 测试和 stcontrol 的 `internal/agent/snapshot.go`、`internal/agent/runtime_state.go`、`internal/store/backups.go` 关闭 R06。Online 六个 stcontrol 套件 30/30 通过，其中实际扩展路径的读写在途计数、单用户隔离、过期自动开门及精确 renew/release 均有断言。`internal/controller/snapshot_process_postgres_integration_linux_test.go` 在 Linux 真实启动 Controller 与两个 Agent 子进程并连接隔离 PostgreSQL，注入 source gate 后崩溃、target 上传中崩溃和同目录重启，最终只发布一份逐字节一致 immutable archive；`internal/controller/backup_postgres_integration_test.go` 另证明用户回归取消在数据库单事务收敛且新 Controller 不重放命令。WSL 精确续约失败/目标 capability 崩溃恢复矩阵和 Linux vet 通过；Windows 连接真实 PostgreSQL 的 `go test ./... -count=1` 全部通过（Agent 216.785s、Controller 51.317s、Store 20.267s），`go vet ./...`、`go build ./cmd/controller ./cmd/agent` 通过。
- `Sillytarven-online` 当前只剩既有未跟踪 `简要开发思路.md`；它按协作规则视为其他工具的 WIP，本阶段未覆盖或提交。
- `stcontrol` 已建立 `flash`（自 2026-08-14 起所有实现均提交推送至 flash 分支）并持续推送至指定远端 `https://github.com/inliver233/stcontrol.git`。

## 2026-08-14 flash 分支阶段记录（P0 修复 + R20/R21 配额下发）

- **P0 WIP SQL 修复**：internal/store/replica_cleanup.go:67,111 与 internal/store/snapshots.go:1072 的 legacy.handle 改为 legacy.username（users 表无 handle 列）；internal/store/storage_repair.go 的 next_attempt_at CASE 表达式把 $1 显式定型为 timestamptz，避免 PostgreSQL 推断为 interval。修复前真实 PostgreSQL 上 TestPostgresCriticalConcurrency、TestPostgresStorageRepairTaskTerminalConstraints、TestControllerSnapshotWorkflowThroughDurableAgentCommands、TestControllerIndependentReconciliationSnapshotsAndCompletesAfterRetry、TestControllerArchiveRestoreThroughDurableAgentCommands 五个集成测试全部失败；修复后全部通过（store 13.9s / controller 12.1s），无 DSN 的默认单元套件全绿。R09/R19 的 storage 修复/副本清理路径恢复可用。
- **R20 bug实测 7 项**：
  1. 空列表 null 崩溃：后端 ListNodes/ListVisibleProtectionAlerts/ListAccountImportClaimTargets 空结果改为返回空切片（make(..., 0)），JSON 输出 [] 而非 null；前端 Admin.tsx（nodes/alerts/admins）、Account.tsx（claims/identities）全部加 ?? [] 防御。
  2. 新增节点入口：Admin.tsx 节点管理页新增“+ 添加节点”向导（名称/角色 compute|storage/地区/酒馆地址/期望配额），调用既有 POST /api/admin/nodes，创建后提示点击“注册令牌”生成安装命令；角色注册后不可修改文案明确。
  3. 登录页管理员入口：Login.tsx 移除“管理员登录”链接；管理员仍可直达 /admin（未登录/非管理员自动跳 /admin/login）。
  4. 节点状态两层显示：Admin.tsx 新增 compositeStatus + StatusBadge，按 已退役→维护/排空→离线→不兼容→已满→繁忙→可用 优先级给出综合状态主徽章，下面小字显示连通性/运营/容量/兼容四维；离线时容量/兼容置灰并标注“（最后上报）/（最后检测）”。
  5. 角色与备份策略文案：按钮按角色区分——计算节点“允许作为热备目标/停止接收热备”，存储节点“接收归档备份/暂停接收归档”，不再用误导性的“设为备份”。
  6. 添加节点角色区分向导：随 #2 一并落地（角色单选+用途说明+安装参数区分）。
  7. 节点配额下发配置：迁移 0041_node_quota_policy.sql 给 nodes 增加 expected_disk_quota_bytes/quota_policy_version/quota_sync_state/quota_sync_at/quota_sync_error_code；Store.UpdateNodeExpectedQuota 单调递增版本；handleAdminUpdateNode/handleAdminCreateNode 接受期望配额；心跳响应回显期望配额+版本（protocol.HeartbeatResponse），Agent 在 heartbeat.go 校验并应用后把生效配额与已应用版本上报（HeartbeatRequest.AppliedQuotaVersion/EffectiveQuotaBytes），心跳 UPDATE 按生效值自动收敛 quota_sync_state（synced/pending/applying/failed）；前端节点行显示“期望/生效/同步状态”，并有“配额策略”编辑面板。
- **R21 说明**：HTTP 层限流（R21/R22 明确要求）仍缺失，属 P2 待办；本轮仅完成配额策略下发闭环与前端配额 UI。


## 2026-08-14 flash 分支阶段记录（P1 安全修复）

- **Online adapter 三处独立模式语义漏洞（R02/R04/R13）**：Sillytarven-online/src/stcontrol.js 的 stcontrolRequestTracker 修复——
  1. 新 envelope 一律以 MANAGED（中性）创建，不再在 independent 模式下直接标记 INDEPENDENT；INDEPENDENT 写权只能由 canUseNativeLogin 归属证明分支授予（原 939 行按模式直接传参导致 945 行归属证明被跳过）。
  2. 纵深防御：INDEPENDENT 写入要求 state.sessions 中存在匹配 handle、未登出的 durable INDEPENDENT 会话，否则 409 stale_writer_session（与既有语义一致，Go 侧协议零变化）。
  3. 写门/租约准入检查移入同一个串行化 mutateState 线性化点，与 in-flight 计数原子化，消除“gate 已建立但写入已放行”的 TOCTOU 竞态；cleanExpiredSessions/cleanExpiredSnapshotGates 保留在同一原子点内执行。HTTP 状态码/响应体不变（423 user_quiescing/user_data_frozen + Retry-After、409 stale_writer_session）。
  测试：tests/stcontrol-adapter.node.test.mjs 新增 independent 无证明 423、伪造 INDEPENDENT envelope 写 409、真实 durable 独立会话放行三条；全套件 node --test tests/stcontrol-*.node.test.mjs 32/32 通过。
- **恢复/冲突发布竞态（R19）**：
  1. internal/store/restores.go CompleteRestoreWorkflow 在串行化事务内（generation 校验后、任何副本晋升前）用 getActivityLeaseForUpdate + leaseBlocksNewWriter 复查用户活动租约；用户已重新活跃/排空/独立时返回 ErrReplicaTakeoverLeaseActive（Controller 映射 409），杜绝恢复发布覆盖活跃写入。新增 TestCompleteRestoreWorkflowRefusesPublishOverActiveWriterLease。
  2. internal/agent/conflict_resolution.go 发布全程持有 replicaMutationMu，并在发布前以 conflictResolutionPublishAllowedLocked 复核写者租约（无租约/已过期/匹配本地会话 epoch 才允许），与副本清理/快照并发序列化。新增无租约放行、活跃租约拒绝、匹配 epoch 放行及 Linux 互斥序列化测试。
  3. web/src/pages/Conflict.tsx 轮询修复：仅 succeeded 视为完成并跳登录；401/403 清定时器跳登录；其他 4xx 仅当响应体声明终态（resolved/succeeded/failed/cancelled）才停止；网络/5xx 继续轮询。web/src/api.ts 的 request() 现把 status/data 附加到 ApiError 供页面区分。


## 2026-08-14 flash 分支阶段记录（P2 安全与灾备）

- **R21 HTTP 限流 + R22 登录锁定**（internal/controller/ratelimit.go、login_lockout.go）：
  1. 每 IP 固定窗口限流（默认 120 req/min，认证端点 20 req/min，键上限 100k 防内存膨胀，过期键每分钟 GC）；`X-Forwarded-For` 仅在直连对端为 loopback/私网时信任，公开客户端无法伪造身份绕过。
  2. 认证路由组（/api/auth/*）挂 loginRateLimitMiddleware + loginLockoutMiddleware；登录/管理员登录成功清零、失败累计，连续 5 次失败后按指数退避锁定（30s 起，上限 15min），`Retry-After` 头返回。
  3. 限流中间件在 Server.New 中初始化，agent/loopback 端点不受影响（Agent 通道有 HMAC/nonce/租约围栏，不应被管理流量饿死）。
  4. 测试：TestRateLimiterAllowsUpToLimitThenRejects、TestRateLimiterRespectsMaxKeys、TestRateLimitMiddlewareRejectsBurst、TestLoginLockoutLocksAfterFailuresAndRecovers、TestLoginLockoutMiddlewareRejectsLockedUsername 全过。
- **R22 审计读侧**（internal/store/audit_events.go、internal/controller/audit_admin.go、web/src/pages/Admin.tsx）：
  1. 新增 ListAuditEventsPage（按 id 游标分页 + actor_type/action/target_type/outcome 过滤，detail 以 JSONB 返回，不落日志）。
  2. 新增 GET /api/admin/audit 管理员端点，审计只读、仅管理员可见。
  3. 前端新增“审计日志”页（筛选/分页/详情展开，空状态明确）。
  4. 测试：TestListAuditEventsPageFiltersAndPaginates、TestListAuditEventsPageEmptyIsEmptySlice 全过。
- **Round 60 总控灾备自动备份**（迁移 0040 + internal/store/controller_disaster_backups.go + internal/controller/controller_backup.go + internal/agent/controller_backup.go）：
  1. 持久化 workflow 状态机（scheduled→snapshotting→transferring→verifying→publishing→succeeded，支持 retry_wait/failed/cancelled/superseded），operation_id 幂等、controller_generation 围栏、lease 认领、DB 内指数退避。
  2. 后台 reconciler 每 5 分钟检查：有合格纯存储节点（storage/online/active/compatible/is_backup_target）且距上次成功超过配置间隔（默认 24h）时调度；pg_dump（PGPASSWORD 环境变量，不经命令行）+ config + manifest 打包 tar.zst，经一次性 capability 传输到目标 Agent，目标校验 sha256+size 后落 BackupDir/controller-backups/<operation_id>/，成功后只保留每节点最新一份。
  3. 管理 API：GET /api/admin/controller-backups（分页列表）、POST /api/admin/controller-backups（立即触发，幂等）。
  4. 配置：controller_backup.enabled/interval_sec/retry_max/keep_latest_only/pg_dump（默认启用、24h、3 次重试、仅留最新、含 pg_dump）。
  5. 测试：store 层 schedule/claim/complete/fail/retention/idempotency sqlmock 测试、controller reconciler 测试、agent 接收路径测试全过。
- **移除硬编码密钥**（run-live-controller.bat、.gitignore）：live 启动脚本不再内嵌 CONTROLLER_SECRET_KEY 与引导密码，改为要求环境变量或 git-ignored .live-secrets.cmd；.gitignore 增加 .live-secrets.cmd、livecheck.log、*.log。


## 2026-08-14 flash 分支阶段记录（P3 部分缺口收敛）

- **R12 离线节点不再永久阻塞控制面**：`IsControlPlaneReady` 只要求在线（connectivity_state='online'）计算节点完成 managed 对账；离线节点持有旧世代凭据重连时只能经 recovery heartbeat 轮换（middleware 围栏保证），不能领取命令，因此不会产生双写。新增 TestControlPlaneReadinessIgnoresOfflineNodes。
- **R15 注册不确定结果有界 + handle 预留 TTL**：`retryRegistrationTransition` 现对所有错误类（node_unavailable/command_enqueue_failed/command_timeout/command_uncertain/publish_failed）在达到 registrationMaxAttempts 时终态失败并释放 handle；新增 `ReleaseExpiredRegistrationReservations`（迁移内 24h TTL，超过即把 workflow 置 failed/reservation_expired 并释放预留），注册 reconciler 每轮先释放过期预留。注册测试更新为断言有界行为。
- **R19 冲突证据失败恢复路径**：`ListConflictEvidenceTasks`/`ClaimConflictEvidenceTask` 现把 evidence_state='failed' 且超过 `ConflictEvidenceFailedRearmWindow`（6h）的来源重新纳入认领，避免节点长时间离线/临时故障导致证据永久失败、用户被永久冻结。新增 TestListConflictEvidenceTasksRearmsFailedSources。
- **R09 存量 hot_standby 无身份文件不再卡死**：Agent 对缺失/非法 `.stcontrol-replica.json` 的来源返回 sentinel `errReplicaIdentityUnavailable`，命令错误码为 `replica_identity_unavailable`；Controller 收到后调用新的 `FailReplicaCleanupTask` 终态失败（副本 copy/user_replicas 回到 stale/ready，不再阻塞该用户快照/恢复），代码进审计与管理员可见错误码，绝不盲删无法验证的目录。新增 TestFailReplicaCleanupReleasesFenceAndRestoresCopyState。


- **R09/R19 离线备份调度不再被单条同步工作流阻塞**：`TriggerUserBackup` 在 snapshot 并发槽饱和时立即返回（工作流保持 scheduled 由 20s durable reconciler 认领续跑），离线备份调度循环可继续扫描所有用户，慢归档不再串行阻塞后续备份；管理员手动触发与断言完成的集成测试仍同步执行（有槽时）。


## 2026-08-14 flash 分支阶段记录（P3 收尾：Round 20/51/52、R18）

- **Round 51/52 无备份保护前端提示**：Account.tsx 现加载 `api.protection()`，按保护状态显示“数据保护：已保护/临时保护/未保护/冲突”提示条；未保护时额外提示“当前还没有可用的备份副本…系统会在检测到合格备份目标后自动为你建立首次同步保护”。
- **Round 20 管理员可调推荐权重**：迁移 0043 给 nodes 增加 `recommendation_weight`（0-100，默认 0）；UpdateNodeSettings 持久化；注册页 `availableNodeRank` 以“可注册+容量开放 > 容量繁忙 > 不可注册”为主序，权重在同类内优先（每单位权重=10 档），前端“配额策略”面板新增推荐权重输入。
- **R18 客户端延迟持久化**：迁移 0044 给 nodes 增加 `client_latency_ms/client_latency_observed_at`；新增 `RecordNodeClientLatency`（EWMA 平滑、0..3600000ms 校验）与 `POST /api/users/me/node-latency`（登录用户、严格 JSON）；Nodes/Register/SelectNode 实测延迟后 fire-and-forget 上报；`availableNodeRank` 在权重同级内以延迟档位（<100/<300/<800ms）作次级排序。测试：store EWMA/校验、controller 有效样本持久化与 6 类非法样本 400。


## 2026-08-14 flash 分支阶段记录（P3 收尾：Round 26、R16）

- **Round 26 管理员可改存储修复目标**：迁移 0045 给 storage_repair_tasks 增加 `preferred_target_node_id`；新增 `SetStorageRepairPreferredTarget`（校验目标是纯存储节点、0 清除、无待办任务返回 ErrNoRows）；`ClaimAndCreateStorageRepair` 目标选择 ORDER BY 优先管理员指定节点（仍强制健康/容量/兼容门禁，不合格自动回落确定性选择）；新增 `POST /api/admin/users/{uuid}/storage-repair-target` 管理端点与 `GetUserByUUID` store 方法。测试覆盖校验/持久化/清除/无任务/非法输入。
- **R16 老账号自动扫描（可选）**：新增 `import_scan` 配置（默认关闭、6h 间隔、每轮最多 2 节点）；`ListUnscannedComputeNodes` 列出从未扫描或最近一次 review 批次早于窗口的在线计算节点；`importScanReconciler` 后台协程用与管理员按钮完全相同的幂等扫描路径（scanAccountInventory + operationID 派生）执行无人值守扫描，失败下轮重试。默认关闭保持管理员完全控制。测试覆盖候选筛选与空结果。


## 2026-08-14 flash 分支阶段记录（Round 61 主密钥恢复材料）

- **Round 61 主密钥恢复材料**：新增 `internal/crypto/master_key_recovery.go`——用恢复口令（scrypt N=2^15 派生 AES-256-GCM）把控制面主密钥密封为可 JSON 序列化的信封（随机 salt、带格式版本与 KDF 参数白名单校验、GCM 认证防篡改）。总控灾备备份在配置 `controller_backup.recovery_passphrase_env`（环境变量名）后，把 `master_key_recovery.json` 写入归档并把信封摘要记入 manifest（归档内绝不存明文密钥；未配置则完全不含密钥材料）。`cmd/controller --recover-master-key <archive>` 用 `CONTROLLER_RECOVERY_PASSPHRASE` 解出 base64 主密钥，供丢失密钥后重建/恢复演练。测试覆盖密封/解封回环、JSON 往返、错误口令/篡改/弱口令/非法 KDF 参数拒绝。


## 2026-08-14 flash 分支阶段记录（R16 分类一致性）

- **R16 oauth/unknown 同名候选不再卡死 claim_required**：`classifyAndLinkImportCandidate` 现对同名冲突区分证明方式——password/mixed 账号走 `claim_required`（节点密码证明），OAuth-only（account_kind=oauth）账号走 `oauth_unmatched`（避免被密码守卫永久卡住）。新增 `ResolveOAuthUnmatchedCandidates`：用户在 OAuth 回调登录成功后，Controller 按每个在线计算节点的节点作用域 HMAC 指纹重算并幂等解析匹配的 `oauth_unmatched` 候选（存在冲突 node_accounts 或非 active 用户则跳过）；`resolvedOAuthUnmatchedAfterLogin` 在 handleOAuthCallback 登录成功后调用。测试覆盖 OAuth-only 同名分类、非 active 用户跳过、匹配解析与批统计更新。

## 2026-08-14 flash 分支阶段记录（R14 密码收敛闭环）

- **R14 Bug 1 旧密码在同步未完成前仍有效**：改密/管理员恢复重置现在把旧 verifier 保留为 `previous_password_hash`（迁移 0042 的 auth_identities.previous_password_hash + password_changed_at），控制面登录在“任一节点账号仍 pending/error 且变更在 48h 窗口内”时接受当前或上一版 bcrypt hash（`internal/store/password_convergence.go` 的 `PasswordFallbackHash`、`internal/controller/user.go` 的 `Server.passwordMatches`）；每节点收敛或窗口过期后 `ClearPasswordFallbackIfConverged` 动态且物理丢弃旧 verifier。测试：`internal/store/password_convergence_test.go`、`internal/controller/password_fallback_test.go`；DSN 门禁集成测试 `password_sync_postgres_integration_test.go` 断言节点 pending 时旧密码可登录、收敛后旧密码 403。
- **R14 Bug 2 解绑不推送到节点**：`UnbindUserIdentity`（provider=password）在同一事务内为每个在线/离线节点插入持久 removal intent（`node_account_password_removals`，迁移 0042）；`reconcilePendingPasswords` 经 `deliverPasswordRemovals` 向可达节点下发 `set_password{Remove:true}`（协议 `SetPasswordRequest.Remove`），节点确认后才清除 intent，离线节点退避重试直到回归。Agent 侧 `internal/agent/commands.go` 的 set_password 校验放行 Remove-only 载荷（无 hash 材料），`internal/agent/tavern.go` 原样转发；Online adapter `src/endpoints/stcontrol.js` 的 `/api/stcontrol/internal/users/password` 在 `remove:true` 时删除本地 password/salt/version verifier。测试：`internal/agent/commands_test.go` 新增 Remove 转发与无 Remove 缺材料拒绝；adapter 套件新增 removal 路径结构断言。
- 验证：`go build ./...`、`go vet ./...`、`go test -short ./...` 全绿；Online `node --test tests/stcontrol-adapter.node.test.mjs` 16/16。提交 `5b33abe`（stcontrol flash）、`b3e5e86`（Sillytarven-online online）。真实节点适配器的端到端解绑链路（真实 adapter 在线接收 Remove）仍待迁移 Linux 后的浏览器/双进程矩阵补测，R14 继续保持 `部分`。

## 2026-08-14 flash 分支阶段记录（AI 监管层 Phase 0–6B）

> 需求变更点：见 `docs/ai-supervision-decisions.md`（用户逐项确认的 7 个变更点：自动降级但保留无 AI 能力；全部接入第三方 API（OpenAI/Responses/Anthropic/Gemini/OpenAI 兼容，URL+Key+Model 用户自定义）；冲突正文特殊情况下系统自判可发受限片段；低风险自动采纳、灾难/身份/接管/冲突发布始终人工；审计黑盒只存脱敏 observation/digest；低频巡检+事件被动触发+去重；模型升级完全用户自定义）。
- **Phase 0 基座**（`internal/ai/`）：`provider.go`（OpenAI Chat Completions / Responses、Anthropic Messages、Gemini generateContent、OpenAI 兼容五类原生格式适配，超时+256KiB 上限+响应解析）、`redaction.go`（HMAC 伪名 ref、5% 指标分桶、secret pattern 扫描与文本清洗）、`observation.go`（脱敏 observation + evidence/candidate catalog + digest）、`schemas.go`（Schema 1.0、7 任务、14 动作白名单）、`prompts.go`（§6.1 系统提示词 + §6.3 七个任务提示词，版本化）、`validator.go`（§5.4 十步校验链：UTF-8/schema/task/observation/action/refs/evidence/候选/secret/abstain）、`supervisor.go`（后台队列、每 provider+model+task 熔断（5 次失败开 10 分钟，half-open 单探针）、shadow/advisory/auto_low_risk 模式、EnqueueTask）。迁移 0046 建 `ai_advisory_requests/advisories/outcomes` 三张审计表（黑盒）。Controller 侧 `ai_supervisor.go` 适配 store、`buildAIObservation` 从真实节点/告警构造巡检 observation，`ai_admin.go` 提供 `/api/admin/ai/status|advisories|requests` 只读端点；config 新增 `ai_supervisor` 段（默认 `enabled:false`，AI off 零行为差异）。Store 层 `ai_advisories.go` 全套持久化（dedup_key 幂等）。
- **Phase 1 监控巡检**：后台 worker 按 `inspect_every_sec`（默认 10 分钟）构造集群聚合 observation → AI 选最值得关注问题 → validator → 落库；Admin.tsx 新增“AI 监管”页（状态/最近建议/调用记录分页，任务与动作中文标签）。AI 关闭时既有 overview/alerts 完全不变。
- **Phase 2 告警归因**：`enqueueAnomalyAttribution` 对未解决告警（按 UserUUID 伪名、summary 清洗）每 5 分钟去重入队；`CountOpenAlertsBySeverity` 提供严重度聚合。
- **Phase 3 节点/备份排序**：`enqueueScheduleRecommendation` 只把确定性层已 eligible（`nodeEligibleForNewLoad`/`nodeEligibleAsBackupTarget` 与既有硬门一致）的候选放入 candidate_catalog；少于 2 个候选不调用。
- **Phase 4 恢复编排**：`enqueueRecoveryPlan` 读取 restore workflow 状态/attempt/error code（从共享 workflows 表）构造恢复编排 observation。
- **Phase 5 导入歧义说明**：`enqueueImportReview` 只对 `claim_required/oauth_unmatched/identity_conflict/recovery_required` 未决候选生成说明请求；不返回 handle（批次伪名）。
- **Phase 6A 灾难判断**：`enqueueDisasterReview` 读取 `node_control_mode_events`（reported/desired/reason/age），`deterministic_hard_floor_satisfied` 仅在节点真实进入 independent/draining 时为 true；提示词规则禁止绕过双信号/最短时长/draining。
- **Phase 6B 冲突元数据建议**：`enqueueConflictReview` 只发送冲突聚合（来源数/文件数/字节/证据是否 ready/年龄），不含路径/正文/digest；`RECOMMEND_CONFLICT_*` 动作恒要求人工确认。冲突正文级预览（§7.7 第二级）默认不启用——仅在服务端标记 preview_eligible 且通过 secret scan 的小型 UTF-8 片段由用户逐案授权，原件永不覆盖；该内容级路径留待 Linux 迁移后的浏览器矩阵。
- **验证**：`go build/vet/test -short ./...` 全绿；真实 PostgreSQL 隔离 schema 上 47 个迁移全部应用，`TestPostgresAIAdvisoryRoundTrip`（幂等 dedup/状态机/黑盒三表闭环）与 `TestPostgresAIObservationAggregates`（五个聚合查询空表安全）通过；web tsc + vitest 16/16。提交：`86f9873`（Phase 0）、`c5304de`（Phase 1 UI）、`09a960a`（Phase 2–6B workers）。真实第三方 provider 影子回放、30 天 shadow 期与非劣阈值评估留待部署后按 §8 发布流程执行；AI 监管层继续保持 `部分`。
- **交付口径（2026-08-15 第二批更新）**：Phase 0–6B 为“观测链路 + 决策④采纳链路完成”：observation 构造、脱敏、validator、审计三表、熔断与 admin 只读端点在位（含 Phase 3/4 candidate_catalog 序列化与保护态真实聚合）；决策⑤ rejected outcome 审计行已接线；**决策④ auto_low_risk 分级采纳链路已于 681b8b1 全量落地**——Supervisor 仅在 auto_low_risk 模式、confidence≥下限、validator 白名单判定可逆且非人工确认任务族时调用确定性执行器（ai_adoption.go），执行器对当前 Store 状态复查全部硬门（排序必须恰好覆盖当前合格节点、部分/陈旧失败关闭；告警说明重跑 secret scan 且只注解被引用告警），效果经迁移 0048 `ai_adoption_effects` 持久化（随 advisory 过期、按 (request,kind) 幂等），消费端只在确定性分层内作次级偏好，不能晋升不合格/隐藏合格节点；shadow/advisory 零效果，人工采纳走同一执行器与门禁（POST /api/admin/ai/advisories/{id}/adopt，accepted/admin 审计行）。真实 PG 覆盖排序硬门/告警说明/模式与置信度门；灾难、身份、接管、冲突发布建议始终无执行器。AI 仍不在任何请求关键路径，off 零行为差异不变。

## 2026-08-14 flash 分支阶段记录（真实 PostgreSQL 复验）

- **R19 冲突证据认领 SQL 占位符修复**：`ClaimConflictEvidenceTask` 的 failed 重新武装子句误用 `$4`（与 evidence_lease_until 冲突），真实 PostgreSQL 报 `got 5 parameters but the statement requires 4`；改为 `$5` 后真实库 conflict evidence→resolution 全链集成测试通过。该缺陷只在真实数据库暴露（sqlmock 不校验占位符数量）。提交 `3eb6ba8`。
- 复验：真实 PostgreSQL 上 store 全部 `TestPostgres*` 与 controller 全部 `TestController*` 集成套件全绿；`go build/vet/test -short ./...`、web tsc + vitest 16/16、Online adapter 16/16 全绿。

## 2026-08-15 flash 分支复审闭环（P1 阻断缺陷）

> 本节以当前代码、最新 requirements-context 矩阵和复审实测为准；它覆盖本文件中更早的阶段性判断。这里仅声明已确认实现缺陷的闭环，不把未运行的真实 PostgreSQL、Linux 双进程或跨机故障矩阵冒充完成。

- **OAuth 登录 CSRF 绑定**：登录 state 除数据库 hash/一次性核销外，新增 provider/path 作用域的 host-only、`HttpOnly`、`SameSite=Lax`、HTTPS 下 `Secure` 浏览器 cookie；callback 在消费 state 前做定长、常量时间 cookie 绑定校验，绑定 OAuth 流程仍沿用既有 Controller session/generation 约束。
- **R14 改密/解绑节点收敛**：Online adapter 接受并精确校验 `remove:true + version`，删除本地 verifier；迁移 0049 只为当前确实无密码材料的账号回填 removal version，有新材料的历史 intent 直接完成。待发 removal 查询同时约束当前账号版本一致且 hash/salt 均为空，避免离线重连时旧解绑与同版本新密码永久冲突。
- **R12 Controller rebuild 与节点世代**：迁移 0050 引入 `deferred/ready_with_deferred`，promotion 原子隔离非活动世代节点；离线或 stale 节点不会永久阻断控制面，但也不会被伪记为 reconciled/succeeded。旧凭据 recovery heartbeat 在任何 Store mutation 前关闭本地 gate，只记录恢复事实、保持节点 offline，且 handoff、注册、命令租约和手工 online 更新均以 active controller generation 做数据库围栏。在线节点随后失联时，stale sweeper 在 serializable 事务中 defer 对应 rebuild item 并重算 readiness。
- **节点兼容性观测**：第三次过早心跳不再写入违反数据库约束的 `verifying/observations=3` 组合；达到时间门槛后才进入 resolved，兼容升级路径保持可重放。
- **独立模式 envelope/租约**：Online 对无 envelope 的 managed/independent 会话失败关闭；managed→independent 必须证明精确 durable ownership，并原子更新 session mode/epoch/generation；draining 只允许已持久化 independent 会话，回到 managed 前会关闭遗留 independent 写门。用户与管理员 handoff 互斥，生产入口明确装载 heartbeat 且管理员专用页不发用户保活。
- **数据故障 gate 可恢复状态机**：迁移 0051 持久化 freeze/release 状态、attempt、operation 与 generation；resolved 但尚未 released/superseded 的 fault 仍阻止新 fault 报告。Controller 对 freeze/release 公平轮转，命令 enqueue 原子校验期望 generation；仅该 generation-fenced 工作流可把 exact failed/expired delivery 换新 command ID 重排，普通固定命令的 replay 语义不扩张。Agent、Controller 与 Online 对 `operation_id/controller_generation/fault/user/handle/activity_epoch` 逐层精确回显和校验；Online 用 fault-scoped durable tombstone 支持远端已释放后的新世代收敛，gate 删除、tombstone 与成功回执同事务持久化。
- **恢复/冲突恢复 UI**：恢复发布和冲突发布继续在最终发布点复查活动写租约；`Conflict.tsx` 只把真实终态视为完成，401/403 才跳登录，404/网络/5xx 保持轮询。刷新后持久 operation 先恢复为 retrying 占位并继续轮询，不再因第一次瞬态失败丢失恢复上下文。
- **R06 直接门禁覆盖**：新增实际命中 `write_drain_timeout` 的用例，验证超时只释放当前 snapshot gate，不能删除 replacement 或 data-fault gate；不再以“仅 Linux 可测”为由遗漏该分支。
- **本机基础证据**：Online P1 定向 Node 套件 44/44；stcontrol web Vitest 19/19、production build 通过；`go test ./...`、`go vet ./...`、`go build ./...` 通过。首次 Go 全量测试使用 C 盘默认 TEMP 时有 3 个容量门禁用例因临时盘仅余约 1.6 GiB 正确失败；将 TEMP/TMP 指向同项目 E 盘测试目录后原命令全绿，证明容量保护本身生效。新增 PostgreSQL 升级、并发和崩溃恢复用例在未配置 `STCONTROL_TEST_POSTGRES_DSN` 的本机明确 skip，保留到 Linux/真实 PostgreSQL 验收执行。

## 完成判定规则

每一行只有同时满足以下条件才可改为 `完成`：

1. 数据模型、后端、Agent、酒馆适配器和前端中适用的实现均已落地；
2. 正常、失败、重试、重复请求、重启和安全边界测试覆盖本行范围；
3. 运行/集成/压测证据与需求规模相匹配；
4. 文档、配置和部署方式没有继续声明已废弃的 demo 语义；
5. 不依赖未提交 WIP 或人工补步骤冒充自动闭环。
