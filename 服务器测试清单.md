# stcontrol / SillyTavern-online 服务器验收测试清单

> 用途：功能开发收尾后，在 Linux 服务器上执行真实 PostgreSQL、跨进程、跨节点、故障注入、容量、安全和浏览器验收。
>
> 原则：每项都必须保存命令、时间、Git SHA、日志和结果；出现 `SKIP` 不算通过。涉及断网、磁盘填满、杀进程和数据库恢复的项目只能在隔离测试环境执行。

## 0. 验收记录

| 项目 | 填写内容 |
|---|---|
| 测试日期 |  |
| 测试负责人 |  |
| stcontrol 分支 / SHA | `flash` / `2f1ee68e6641d5d4b50bcca96bc55ac1a7bef93b` |
| SillyTavern-online 分支 / SHA | `online` / `b4dd65b47c178ea87ea42f56ac1b7ef1a8a934cd` |
| Linux 发行版 / 内核 |  |
| Go / Node / npm 版本 |  |
| PostgreSQL / pg_dump 版本 |  |
| Controller 地址 |  |
| 计算节点 |  |
| 存储节点 |  |
| 被动 Controller |  |
| 证据目录 |  |

最终结论只能填写：`PASS`、`FAIL` 或 `BLOCKED`。任何必测项未执行、被跳过或无证据时，不得填写 `PASS`。

## 1. 推荐隔离拓扑

- `CTRL-A`：活动 Controller。
- `CTRL-B`：被动/恢复 Controller，用于世代竞争和接管。
- `PG-A`：PostgreSQL 17，独立测试数据库。
- `C1/C2/C3`：至少三个计算 Agent；C1/C2 运行 SillyTavern-online，C3 用于见证/隔离场景。
- `S1/S2`：至少两个纯存储 Agent，用于 archive 重建、存储退休和故障迁移。
- `LOAD`：独立压测/浏览器主机，避免压测进程与 Controller 抢资源。
- 所有公网或跨机 Controller/Agent 数据面地址使用受信证书和 TLS 1.3；HTTP 仅允许 loopback 测试。

## 2. 前置检查

- [ ] 两仓库没有未合并文件、rebase、merge 或冲突标记。
- [ ] 测试 SHA 已固定并记录，服务器测试期间不直接修改生产文件。
- [ ] PostgreSQL、Agent 数据目录、SillyTavern 数据目录均为可丢弃测试数据。
- [ ] 已准备数据库和目录级备份，并验证回滚方法。
- [ ] 时间同步正常；所有节点时钟误差小于 2 秒。
- [ ] 防火墙仅开放预期的 Controller、Agent 数据面和 SillyTavern 端口。
- [ ] `CONTROLLER_SECRET_KEY`、数据库密码、bootstrap 密码和见证 PSK 只通过环境/0600 文件提供，不写入仓库或测试日志。
- [ ] 测试机具备 `go`、`node`、`npm`、`git`、`openssl`、`curl`、`jq`、`pg_dump`、`psql`、`zstd`。
- [ ] 故障注入机另备 `tc`/`nft` 或 `iptables`、`stress-ng`/`fio`；命令先在隔离网段演练。

建议先创建证据目录：

```bash
export EVIDENCE="$PWD/server-test-evidence/$(date -u +%Y%m%dT%H%M%SZ)"
mkdir -p "$EVIDENCE"
git -C stcontrol rev-parse HEAD | tee "$EVIDENCE/stcontrol.sha"
git -C Sillytarven-online rev-parse HEAD | tee "$EVIDENCE/online.sha"
go version | tee "$EVIDENCE/go-version.txt"
node --version | tee "$EVIDENCE/node-version.txt"
npm --version | tee "$EVIDENCE/npm-version.txt"
psql --version | tee "$EVIDENCE/postgres-version.txt"
pg_dump --version | tee "$EVIDENCE/pgdump-version.txt"
uname -a | tee "$EVIDENCE/uname.txt"
```

## 3. 源码、构建和基础测试门禁

### 3.1 仓库完整性

```bash
git -C stcontrol status --short --branch | tee "$EVIDENCE/stcontrol-status.txt"
git -C Sillytarven-online status --short --branch | tee "$EVIDENCE/online-status.txt"
! grep -RInE '^(<<<<<<<|=======|>>>>>>>)' stcontrol Sillytarven-online \
  --exclude-dir=.git --exclude-dir=node_modules --exclude='*.md'
```

- [ ] 两仓库状态符合预期，没有未解决冲突。
- [ ] `flash`、`online` 的 SHA 与验收记录一致。

### 3.2 stcontrol Go / Web

```bash
cd stcontrol
go mod verify 2>&1 | tee "$EVIDENCE/go-mod-verify.log"
go vet ./... 2>&1 | tee "$EVIDENCE/go-vet.log"
go build ./... 2>&1 | tee "$EVIDENCE/go-build.log"
go test -short -count=1 ./... 2>&1 | tee "$EVIDENCE/go-short.log"

cd web
npm ci 2>&1 | tee "$EVIDENCE/web-npm-ci.log"
npm test 2>&1 | tee "$EVIDENCE/web-test.log"
npm run build 2>&1 | tee "$EVIDENCE/web-build.log"
```

- [ ] 所有命令退出码为 0。
- [ ] Go 测试无失败；Web 测试和生产构建通过。

### 3.3 SillyTavern-online adapter

```bash
cd Sillytarven-online
npm ci 2>&1 | tee "$EVIDENCE/online-npm-ci.log"
node --test \
  tests/stcontrol-adapter.node.test.mjs \
  tests/stcontrol-agent-auth.node.test.mjs \
  tests/stcontrol-disaster-login.node.test.mjs \
  tests/stcontrol-heartbeat.node.test.mjs \
  tests/stcontrol-native-entry-guards.node.test.mjs \
  tests/stcontrol-routes.node.test.mjs \
  2>&1 | tee "$EVIDENCE/online-stcontrol-tests.log"
```

- [ ] 六套 adapter 测试全部通过，0 skip / 0 fail。
- [ ] 对本次改动文件执行 ESLint；既有全仓 lint 债务与新增错误分开记录。

## 4. 真实 PostgreSQL 自动化验收

创建专用数据库用户和数据库，不要复用生产库：

```bash
export STCONTROL_TEST_POSTGRES_DSN='postgres://<test_user>:<test_password>@127.0.0.1:5432/stcontrol_test?sslmode=disable'
export STCONTROL_TEST_PG_BIN="$(command -v pg_dump)"
export STCONTROL_TEST_TAVERN_ROOT="$PWD/../Sillytarven-online"
cd stcontrol
```

### 4.1 全量真实数据库测试

```bash
go test -p 1 -count=1 -v ./... 2>&1 | tee "$EVIDENCE/go-full-postgres.log"
```

> 必须使用 `-p 1`：真实进程验收和 Store 集成测试共享 PostgreSQL
> Controller advisory-lock 命名空间；包级并行会制造测试夹具之间的假领导锁竞争，
> 不是生产并发模型。

- [ ] 日志中没有 `SKIP`、`FAIL`、panic、goroutine leak 或未回收测试 schema。
- [ ] 0001 至最新编号的迁移连续应用。
- [ ] 真实 PostgreSQL SQL 路径没有只依赖 sqlmock 绿灯。

### 4.2 核心 PostgreSQL / HTTP / pg_dump 用例

```bash
go test -count=1 -v ./internal/store ./internal/controller \
  -run 'TestPostgres|TestController' \
  2>&1 | tee "$EVIDENCE/postgres-controller-focused.log"
```

- [ ] 并发租约、注册预留、容量、AI 观测/采纳、副本清理通过。
- [ ] 管理员、Agent、OAuth、注册、导入、handoff HTTP 闭环通过。
- [ ] snapshot、restore、conflict、retirement、integrity、data-fault 闭环通过。
- [ ] `TestControllerDisasterBackupRunsRealPgDumpEndToEnd` 使用真实 `pg_dump`，不得 skip。

## 5. Linux、Race、覆盖率和真实进程

### 5.1 Race

```bash
cd stcontrol
go test -p 1 -race -count=1 ./... 2>&1 | tee "$EVIDENCE/go-race.log"
```

- [ ] 0 race report。
- [ ] 0 skip；若因环境跳过，修复环境后重跑。

### 5.2 覆盖率门禁

```bash
go test -p 1 -count=1 -coverpkg=./... -covermode=atomic \
  -coverprofile="$EVIDENCE/coverage.out" ./... \
  2>&1 | tee "$EVIDENCE/go-coverage.log"
go tool cover -func="$EVIDENCE/coverage.out" | tee "$EVIDENCE/coverage-func.txt"
```

- [ ] `total` 覆盖率达到项目最终门禁 80% 以上。
- [ ] 关键安全/恢复状态机不是只靠无断言执行拉高覆盖率。

### 5.3 Linux 文件系统与进程崩溃矩阵

```bash
go test -count=1 -v ./internal/agent \
  -run 'TestSnapshotPublish|TestAgentRestart|TestSnapshotFileWrite|TestSnapshotUnsupported|TestSnapshotPublishesLarge|TestRunSnapshot|TestAbortBackup|TestRunRestore|TestConflictResolution' \
  2>&1 | tee "$EVIDENCE/linux-agent-filesystem.log"

go test -count=1 -v ./internal/agent ./internal/controller \
  -run 'TestControllerAgentTavernProcessesRecoverAcrossRestarts|TestSnapshotWorkflowRecoversSourceAndTargetProcessCrashes|TestGoAgentCalls' \
  2>&1 | tee "$EVIDENCE/process-e2e.log"
```

- [ ] `RENAME_EXCHANGE` 原子发布、每个崩溃边界恢复通过。
- [ ] 内核磁盘满测试真实触发 `ENOSPC`，不是 mock。
- [ ] 大归档测试完成且没有 partial replica。
- [ ] source/target/Controller/SillyTavern 进程分别被杀死后能够恢复。
- [ ] Go Agent 调用真实 adapter 和完整 SillyTavern server，两项均不得 skip。

## 6. TLS、enrollment、凭据与控制通道

- [ ] Controller 跨机监听无证书时拒绝启动；loopback HTTP 仍可用于本机回源。
- [ ] Controller 和 Agent 数据面只协商 TLS 1.3；证书主机名错误、过期、未知 CA 均失败。
- [ ] enrollment token 单次使用；错 role、错 fingerprint 不消费 token。
- [ ] Agent PSK 只以 hash/密文落库，日志中无 token/PSK。
- [ ] pending successor 只能 confirm；激活后旧 PSK 立即失效。
- [ ] HMAC 覆盖 method/path/time/nonce/body；相同 nonce 首次即使返回 400，第二次仍为 401。
- [ ] generation 回退、错 worker ACK、结果重放全部失败关闭。
- [ ] 22 个固定能力拒绝畸形输入；`arbitrary_shell` 返回 `unsupported_command`。
- [ ] Controller 短暂不可达时 Agent 不开启新登录/备份；恢复后队列继续。
- [ ] 记录证书链、握手输出、Agent/Controller 日志脱敏证据。

## 7. 用户、身份和浏览器端到端

使用 Chromium、Firefox 和 WebKit/真实 Safari（可用时），桌面和窄屏分别执行。

### 7.1 注册和登录

- [ ] 用户选择合格节点注册；邀请码缺失、错误、过期和策略读取失败均拒绝。
- [ ] 注册响应丢失后使用相同 operation 恢复；同 operation 修改正文返回冲突。
- [ ] 供应中途杀死节点/adapter，恢复后只创建一个全局用户和一个本地账号。
- [ ] 登录票据仅在 POST body；浏览器地址、历史、Referer 和日志均无票据。
- [ ] 短码错节点不消费，正确节点单次消费，重复消费拒绝。
- [ ] 旧/闲置浏览器页面超过活动期限后必须重新登录。

### 7.2 OAuth、密码和身份同步

- [ ] LinuxDo/Discord 新用户、既有用户登录、绑定和解绑。
- [ ] OAuth state 绑定发起浏览器/session；另一浏览器不能完成回调。
- [ ] provider 失败、回调重放、错误 guild、最后身份保护符合预期。
- [ ] 改密先使全局旧密码失效；一台节点离线/失败时保留版本化收敛事实。
- [ ] 离线节点恢复后使用完全相同的 scrypt hash/salt/version 收敛。
- [ ] password removal 与后续重新绑定/改密并发时，旧 removal 不能删除新材料。
- [ ] OAuth 本地身份增删发生部分命令失败时可持久重试收敛。

### 7.3 管理员与账号导入

- [ ] 多个同级管理员并发改动、禁用、重置密码；最后管理员保护生效。
- [ ] 节点管理员票据与用户票据类型不能混用。
- [ ] 权限降级/撤销后，未消费管理员票据全部失效。
- [ ] 六类导入候选分类正确；同名密码账号必须证明控制权。
- [ ] 多节点同名候选并发认领只产生一个 UUID 映射。
- [ ] 候选合并 UI 支持加载、空态、错误、重试和持久 operation。

## 8. 快照、恢复、冲突、清理和节点生命周期

### 8.1 快照与存储保护

- [ ] 只冻结目标用户；同节点其他用户仍可写。
- [ ] drain 超时自动安全开门，未发布临时目录被清理。
- [ ] manifest、归档和逐文件摘要一致；额外文件/符号链接/设备文件拒绝。
- [ ] 压缩炸弹、文件数、单文件大小、总字节和磁盘余量门禁实际触发。
- [ ] 直连成功优先；直连明确不可达后才切换加密 relay。
- [ ] relay 看不到用户明文，完成后 ciphertext 删除，capability 重放拒绝。
- [ ] 新 archive 发布成功前旧副本保持 ready。
- [ ] 稳定后旧物理 archive 与临时计算副本按持久任务安全清理。

### 8.2 恢复、冲突和数据故障

- [ ] storage-only 恢复先供应账号，再传输/校验/发布，成功前不能登录目标。
- [ ] 恢复最终发布前重新检查活动 writer lease。
- [ ] 真实分叉冻结用户，普通 session 失败，专用冲突恢复区可用。
- [ ] 证据捕获、分页、选择来源、保留双份和最终原子解冻完整执行。
- [ ] conflict publish 与 snapshot/cleanup 并发时由同一 mutation 锁序列化。
- [ ] data fault freeze/release 精确绑定 generation/operation；节点/worker 重启后继续。
- [ ] 深检确认损坏才标 corrupt；轻检异常不能提前毁掉唯一副本。

### 8.3 节点退休/升级/损坏

- [ ] compute 退休逐用户迁移，离线/失败项可重试且无双 home。
- [ ] storage 退休从健康 home 重建到另一 pure-storage 节点。
- [ ] 退休过程中 Controller、source、target 分别崩溃后可恢复。
- [ ] 升级后相同 fingerprint 三次严格递增、跨 30 秒兼容心跳才重新开放。
- [ ] 损坏节点隔离后不参与注册、恢复目标或新备份调度。

## 9. 灾难模式、网络分区和 Controller 恢复

所有场景都要记录两端状态、数据库事实和用户实际写入结果。

- [ ] 短失联：已有合法 session 可按合同工作，新登录/选点/备份暂停，不进入 independent。
- [ ] 缺见证、见证不足、身份重复或任一合法同伴仍能访问 Controller：禁止 independent。
- [ ] 三节点持续确认 Controller 失联达到阈值后，才允许进入 independent。
- [ ] owner adapter 活跃时，用户必须返回原 owner。
- [ ] 整台 owner Agent 丢失时，经本地密码和风险确认、多数 CAS 后仅一个节点接管。
- [ ] A/B 网络分区同时尝试写：不能形成两个合法 writer；异常写固化 conflict 并冻结。
- [ ] 对网络加入延迟、丢包、乱序和抖动，状态机不能在 managed/independent 间振荡。
- [ ] Controller 新世代恢复后进入 `independent-draining`，排空并完成 reconciliation 后才回 managed。
- [ ] 独立写回归 snapshot 在响应丢失、Controller 重启、任务终态失败后能有界重建。
- [ ] 活动/被动 Controller 同时竞争，只有更高合法 generation 成为活动主控。
- [ ] 执行真实 PostgreSQL 备份还原；恢复 Controller 配置和 master-key material 后能够解密必要机器密钥。
- [ ] 旧 Controller、旧凭据和旧 generation 无法签发票据、续租或发布状态。

## 10. 容量、性能与控制/数据面隔离

测试数据必须是生成数据，不使用真实用户内容。

- [ ] 持久建立至少 10,000 个注册/导入候选，并通过 cursor 无遗漏分页。
- [ ] 至少 1,000 个客户端并发登录/心跳达到预期成功率和延迟目标。
- [ ] 单计算节点数百在线用户时，CPU/内存/磁盘/队列指标持续上报。
- [ ] 约 50% 进入 busy 降权；关键资源持续达到约 60% 后 full；恢复滞后和冷却符合配置。
- [ ] 磁盘、配额、在线人数、任务队列硬门立即禁止新注册，但不中断已有用户。
- [ ] 1,000 在线事实下 `CreateRegistrationWorkflow` 仍在事务内拒绝 full/unknown 节点。
- [ ] 两节点各 32 条交错控制命令保持按节点隔离和 FIFO。
- [ ] 大文件传输打满限速时，心跳、登录、管理 API 和 command ACK 仍可用。
- [ ] 每 IP 120/min 全局 API 限流、20/min 登录限流和用户名锁定生效。
- [ ] 伪造 `X-Real-IP`/公网 `X-Forwarded-For` 不能轮换限流 key；可信私网代理场景解析正确。
- [ ] 保存压测工具脚本、并发数、请求率、P50/P95/P99、错误率、CPU、内存、磁盘和网络图表。

## 11. 安全、隐私和日志扫描

- [ ] Controller、Agent、SillyTavern、反向代理、PostgreSQL 日志全量扫描。
- [ ] 日志不含密码、OAuth code/state、API key、PSK、bearer、capability、短码、聊天正文或 archive 内容。
- [ ] URL/query/access log 不含秘密；敏感 query 统一显示为 redacted。
- [ ] 数据目录、临时目录、私钥、环境文件权限符合 0700/0600 约束。
- [ ] archive 路径穿越、绝对路径、UNC、drive、ADS、反斜杠混合穿越、符号链接、非普通文件全部拒绝。
- [ ] CSRF、Origin、cookie SameSite/Secure/HttpOnly、no-store、no-referrer 符合预期。
- [ ] 用户 API 不泄露内部节点拓扑、snapshot ID、capability、文件名或密文材料。
- [ ] 管理员/普通用户/冲突恢复 session 的权限矩阵逐路由验证。
- [ ] 扩展/插件绕过原生登录、注册、改密、删号和写门的尝试全部失败。

可用本地秘密扫描工具，但规则和误报处置必须随证据保存。例如：

```bash
grep -RInaE '(password|api[_-]?key|authorization|bearer|oauth.*(code|state)|capability|psk)' \
  "$EVIDENCE" > "$EVIDENCE/sensitive-scan-candidates.txt" || true
```

逐条人工复核；不能仅以 grep 无输出证明安全。

## 12. 管理端与用户端浏览器矩阵

- [ ] 页面：登录、注册、账号、选点、节点、恢复、冲突、管理员总览/节点/用户/备份/告警/审计/管理员/AI。
- [ ] 每页：首次加载、空数据、分页、筛选、刷新、API 4xx/5xx、网络断开、重试、权限拒绝。
- [ ] 所有按钮只在后端状态允许时启用；重复点击和刷新不会创建重复 operation。
- [ ] 进度页面在 Controller/浏览器重启后从持久 operation 恢复。
- [ ] 320/375/768/1024/1440 px 宽度无横向遮挡，触屏可操作。
- [ ] Chromium、Firefox、WebKit 至少各跑一轮关键链路。
- [ ] 浏览器控制台无未处理异常、React key/null 崩溃或秘密输出。
- [ ] 保存视频/截图、HAR（脱敏）、浏览器控制台和后端关联日志。

## 13. 长稳和升级测试

- [ ] 72 小时 soak：周期心跳、备份、integrity、cleanup、AI supervision、告警无任务泄漏。
- [ ] Controller 和各 Agent 每种角色至少滚动重启三次。
- [ ] PostgreSQL 临时重启/主从切换期间不产生双 writer 或丢失 durable operation。
- [ ] 从上一发布版本按顺序升级到当前版本；迁移只执行一次并可重启续跑。
- [ ] 节点版本不兼容时隔离，升级验证完成后按三次心跳门禁恢复。
- [ ] 数据库、task/workflow、临时文件、goroutine/FD、磁盘占用保持有界。

## 14. 最终发布判定

### 必须全部满足

- [ ] 第 3～5 节自动化门禁全绿且 0 skip。
- [ ] 真实 PostgreSQL、真实 pg_dump、Linux 原子发布、进程崩溃和跨仓 adapter E2E 全绿。
- [ ] Race 0 报告；覆盖率达到项目 80% 门禁。
- [ ] TLS/NAT、网络分区、owner 丢失、被动双主、断电/磁盘满、恢复排空全部有证据。
- [ ] 10k 注册事实、1k 并发 HTTP、备份吞吐与控制面隔离达到目标。
- [ ] 浏览器、权限、安全、日志扫描和响应式矩阵无阻断问题。
- [ ] 所有失败项已有修复提交并完整回归，不以“偶现”或重跑通过直接关闭。

### 验收结果

| 类别 | PASS | FAIL | SKIP/BLOCKED | 证据路径 |
|---|---:|---:|---:|---|
| 构建/基础测试 |  |  |  |  |
| PostgreSQL/迁移 |  |  |  |  |
| Linux/Race/覆盖率 |  |  |  |  |
| TLS/控制通道 |  |  |  |  |
| 身份/浏览器 |  |  |  |  |
| 快照/恢复/冲突 |  |  |  |  |
| 灾难/分区/恢复 |  |  |  |  |
| 容量/性能 |  |  |  |  |
| 安全/隐私 |  |  |  |  |
| 长稳/升级 |  |  |  |  |

最终结论：`________________`

签字：测试负责人 `________________`　发布负责人 `________________`
