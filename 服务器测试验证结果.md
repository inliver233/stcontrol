# stcontrol / SillyTavern-online 服务器测试验证结果

## 1. 最终结论

最终验收结论：**FAIL**。

这不是“当前生产 Controller 已损坏”。相反，本机能够直接完成的源码修复、测试门禁、生产备份、Nginx、时间同步、凭据轮换、镜像扫描、部署、重启恢复和数据清理均已完成，当前 Controller 与 PostgreSQL 正常运行，公网 `/api/health` 返回 204。结论仍是 FAIL，是因为原清单规定“任何必测项未执行、被跳过或无证据时不得 PASS”，而以下事项需要 Cloudflare/另一仓库权限或真实多机环境，不能用单机自动化结果冒充：

1. Cloudflare 公网边缘仍接受 TLS 1.2，不符合本清单“只允许 TLS 1.3”的明确要求。
2. Cloudflare Web Analytics 会针对真实浏览器自动注入 `beacon.min.js`；应用的严格 CSP 正确阻止它，但三浏览器公网矩阵因此产生 15 个 console error group。
3. `Sillytarven-online` 的远端 `online` 分支生产依赖审计仍为 56 个漏洞；本机已有经过测试且审计为 0 的候选补丁，但没有获得对另一个仓库提交/推送的明确授权。
4. Git 完整历史仍有一个历史 Controller secret 形态候选。当前生产 Controller secret 和数据库密码已经轮换，但历史重写会同时影响 `flash`、`server`、其他 clone 和标签，不能未经协同直接 force-push。
5. 当前只有一台真实服务器，无法证明清单要求的主动/被动 Controller、三台 compute Agent、纯存储 Agent、真实 NAT/分区/双主竞争、1,000 个独立登录/心跳会话和上一版本滚动升级。
6. 没有生产管理员、用户、LinuxDo/Discord provider 凭据，因此不能在真实生产数据上执行完整的登录、注册、OAuth、管理员和账号导入浏览器旅程。
7. 宿主机当前实际只公开监听 22/80/443，Controller 8443 只绑定 loopback；但 nftables 的 INPUT 基础策略仍是 accept。生产防火墙的持久 default-deny 应在确认 SSH 管理来源后实施。

用户明确豁免本轮 72 小时稳定性测试，该项记为 **WAIVED**，不会被伪写为 PASS。除 72 小时测试以外的未执行项目仍按 FAIL/BLOCKED 处理。

当前生产状态摘要：

- Controller 镜像：`sha256:4be4f677ff45088e16592e785626c1aaee7c6d8de5be18d7af190021d4a8d117`，running。
- Controller binary SHA-256：`a8fff95929c2bf4aa37472da2f20444a9188ef9b344c40bf6865d148f6508147`。
- PostgreSQL：running / healthy；迁移 `52/52`。
- 清理演示数据后的核心计数：nodes/users/global_users = `0/0/0`。
- origin、Nginx 回源入口和公网 `/api/health`：均为 HTTP 204。
- 当前生产 `.env`、Compose 配置和 TLS 私钥为 0600，公开证书为 0644；当前数据库密码和 Controller secret 已安全轮换。
- 生产备份已在全新 PostgreSQL 17 实例恢复验证，旧镜像回滚点仍保留。

## 2. 固定版本、环境与交付范围

测试日期：2026-08-24，时区 Asia/Shanghai；证据统一使用 UTC 时间。

| 项目 | 固定值 / 结果 |
|---|---|
| stcontrol 原始基线 | `flash` / `2f1ee68e6641d5d4b50bcca96bc55ac1a7bef93b`，同步时等于 `origin/flash` |
| stcontrol 交付分支 | `server`；本轮最终提交和远端 SHA 以第 9 节推送复核为准 |
| 本次收尾前远端 server SHA | `0cfaceccf49635870c2eb8ad13bc7111c75d0c05` |
| SillyTavern-online 固定远端 | `online` / `b4dd65b47c178ea87ea42f56ac1b7ef1a8a934cd` |
| 主机 | Debian GNU/Linux 13，Linux 7.1.8 x86_64，4 CPU，约 5.8 GiB RAM |
| 工具链 | Go 1.26.7；Node 20.20.2；npm 10.8.2；Docker 29.7.2 |
| PostgreSQL 工具 | psql / pg_dump 17.11；测试与生产均为 PostgreSQL 17 |
| 入口组件 | Nginx 1.26.3；chrony 4.6.1 |
| 浏览器 | Playwright 1.62.0 的 Chromium、Firefox、WebKit |
| 最终镜像运行层 | Alpine 3.24.1；构建基础镜像均由 digest 固定 |

用户自己的未跟踪文件 `scripts/seed-demo.sql`、`scripts/clear-demo.sql` 没有加入 Git、没有修改、不会被本次提交覆盖。`clear-demo.sql` 只在备份恢复副本成功演练后用于清理生产演示数据。

`/root/Sillytarven-online` 保留 12 个 staged 候选改动；它们属于另一个仓库，没有被暗中提交或推送。其脱敏 patch 已保存在先前证据目录中。

## 3. 证据目录与可复现性

本次任务共保留四轮证据：

- `server-test-evidence/20260824T005513Z/`：环境、首轮安全扫描、跨仓候选 patch、初始生产状态。
- `server-test-evidence/20260824T065201Z/`：第二轮完整 PostgreSQL、coverage、race、Web 和构建证据。
- `server-test-evidence/20260824T074903Z/`：80% 覆盖率门禁关闭后的权威回归。
- `server-test-evidence/20260824T094125Z/`：最终源码、完整 race、最终镜像扫描、生产部署、备份恢复、浏览器矩阵、重启和主机状态。

最终轮关键证据：

- [Go PostgreSQL 全量日志](server-test-evidence/20260824T094125Z/go-test-postgresql.log)
- [Go 完整 race 日志](server-test-evidence/20260824T094125Z/go-test-race.log)
- [覆盖率精确结果](server-test-evidence/20260824T094125Z/go-coverage-summary.txt)
- [最终系统状态](server-test-evidence/20260824T094125Z/final-system-state.txt)
- [TLS 与公网边缘](server-test-evidence/20260824T094125Z/final-tls-edge.txt)
- [备份恢复](server-test-evidence/20260824T094125Z/backup-restore-final.txt)
- [浏览器矩阵摘要](server-test-evidence/20260824T094125Z/browser-matrix-final-summary.txt)
- [最终静态负载](server-test-evidence/20260824T094125Z/load-final.txt)
- [Cloudflare 精确修复步骤](server-test-evidence/20260824T094125Z/cloudflare-remediation.md)
- [最终 SBOM](server-test-evidence/20260824T094125Z/controller-final.cdx.json)

大日志和 coverage profile 保留原始输出；临时错误 DSN、重复 profile、已被最终结果取代的浏览器截图和一次错误命令名预检已删除，避免把无诊断价值的重复文件推入 Git。真正暴露产品缺陷的修复前 race 失败日志仍保留为 `go-test-race-before-empty-array-fix.log`。

## 4. 按原清单逐章判定

结果含义：PASS 表示有本轮证据；PARTIAL 表示自动化部分通过但清单还要求真实环境；BLOCKED 表示当前权限/拓扑不能执行；WAIVED 仅用于用户明确豁免的 72 小时测试。

| 清单章节 | 结果 | 最终事实 |
|---|---|---|
| 2 前置检查 | PARTIAL | 备份/恢复、时间、权限、工具、端口已通过；宿主防火墙实际暴露正确但基础 INPUT 策略非 default-deny |
| 3 源码/构建 | PASS（stcontrol） | mod verify、vet、build、short race、Web、Compose、actionlint 均通过；跨仓远端固定 SHA |
| 3.3 Online adapter | PASS（功能）/ FAIL（远端依赖） | 固定远端六套 adapter 47/47；远端生产依赖仍有 56 个漏洞，本机候选为 0 |
| 4 真实 PostgreSQL | PASS | 1,775 RUN / 1,775 PASS / 0 fail / 0 skip；真实 pg_dump 与全部迁移路径执行 |
| 5 Race/覆盖率/进程 | PASS | 完整非 short race 全包通过；14,214/17,763 = 80.020267%；真实跨进程恢复通过 |
| 6 TLS/凭据/控制通道 | PARTIAL FAIL | Controller origin 和 Nginx 只允许 TLS 1.3，控制通道自动化通过；Cloudflare 边缘仍允许 TLS 1.2；无真实跨机 Agent 握手 |
| 7 用户/身份/浏览器 | PARTIAL | HTTP/Store/跨进程自动化通过；没有生产账号/provider，不能做真实认证旅程 |
| 8 快照/恢复/冲突/生命周期 | PASS（自动化/本机进程） | 原子发布、ENOSPC、大归档、恢复、冲突、data-fault、清理、退休和重试均通过 |
| 9 灾难/网络分区 | PARTIAL | 双 Agent/双 Tavern/Controller 进程故障恢复通过；无真实多主机分区、NAT、见证与被动双主 |
| 10 容量/性能 | PARTIAL | 10k 候选、1k 容量事实、静态 HTTPS 10k 请求/1k 并发通过；未做 1k 独立认证会话/心跳 |
| 11 安全/隐私/日志 | PARTIAL FAIL | 当前镜像、Web、应用头、Nginx 日志、当前凭据通过；Online 远端漏洞、历史 secret、Cloudflare 两项未关闭 |
| 12 浏览器矩阵 | PARTIAL FAIL | origin 105/105 无错误；公网 105/105 页面成功但 Cloudflare 注入造成 15 个 console error group；无认证旅程 |
| 13 长稳/升级 | WAIVED / PARTIAL | 72 小时由用户豁免；Controller 三轮重启和 PostgreSQL 重启通过；未做上一发布版本顺序升级 |
| 14 最终发布判定 | FAIL | 存在上述外部和真实拓扑必测缺口，按清单规则不能签 PASS |

## 5. 权威测试结果

### 5.1 Go、PostgreSQL、race 与覆盖率

最终权威 PostgreSQL 命令使用独立 PostgreSQL 17、真实 `pg_dump`、固定 Online clone、`-p=1 -count=1 -v -coverpkg=./... -covermode=atomic`。结果：

- RUN 事件：1,775。
- PASS 事件：1,775。
- FAIL：0。
- SKIP：0。
- package fail：0。
- 迁移：0001 至 0052 连续执行。
- 真实 Controller–Agent–SillyTavern 进程恢复 E2E：PASS。

最终完整 race 不是 short 替代品，而是 `go test -p=1 -race -count=1 ./...`：

- `internal/agent`：284.173 秒。
- `internal/controller`：241.187 秒。
- `internal/store`：87.193 秒。
- `internal/crypto`：15.785 秒。
- 9 个 Go 包全部通过，0 race report。

coverage profile 使用 `-coverpkg=./...` 时会为同一源码 block 产生跨 package 重复项。新的门禁脚本按“源码区间 + statement 数”去重，并取各重复 block 的最大执行计数，直接用整数比较，不依赖 `go tool cover` 的一位小数显示：

| 指标 | 结果 |
|---|---:|
| covered statements | 14,214 |
| total statements | 17,763 |
| 精确覆盖率 | 80.020267% |
| 门禁 | 80.000000% |
| 整数余量 | 3 statements |

余量很小，因此新增生产语句时 CI 可能立即失败；应增加有业务断言的测试，不应降低阈值或依赖四舍五入。

### 5.2 静态检查、Web、跨仓 adapter

- `go mod verify`、`go vet ./...`、`go build ./...`：PASS。
- coverage parser 自测：PASS，包含精确 80%、79.992001% 拒绝、坏 header、越界阈值。
- Web：4 文件、25 tests 全部通过；Vite 生产构建通过；生产依赖 audit 为 0。
- Docker Compose config：PASS。
- GitHub Actions workflow：actionlint 1.7.12 PASS。
- 固定远端 Online 六套 adapter：47 pass、0 fail、0 skip。
- CI 显式执行 Online `postinstall`；只做 `npm ci --ignore-scripts` 会缺少默认 `config.yaml`，因此不能省略此步骤。
- 本机 Online 候选依赖审计：0；远端 `online` 干净 clone 审计：56（4 critical、16 high、35 moderate、1 low）。

### 5.3 Linux 文件系统、故障与恢复

已执行并通过：

- Linux `RENAME_EXCHANGE` 原子发布及崩溃边界恢复。
- 真实内核 ENOSPC 传播。
- 大归档发布且无 partial replica。
- source/target Agent、Controller 和 SillyTavern 子进程终止后的恢复。
- snapshot、restore、conflict resolution、data-fault、replica cleanup/integrity、node retirement。
- Controller 连续重启三次，generation 10→11→12→13；每次恢复后 health 204。
- PostgreSQL 重启一次，数据库恢复 healthy，Controller 自动恢复 health 204。
- 重启过程中出现的 broken pipe 和 PostgreSQL administrator termination 是故障注入预期信号；恢复后没有 Controller panic/fatal/error-level 日志。

### 5.4 TLS、HTTP 头、Nginx 与时间

源站结果：

- Controller 8443：TLS 1.3 / `TLS_AES_128_GCM_SHA256` 成功；TLS 1.2 明确收到 protocol version alert。
- Nginx 443 和 Nginx→Controller 回源均固定 TLS 1.3。
- HTTP 重定向使用 `$uri`，不会把不可信 query 带入 Location。
- access log 使用 `$uri` 而非 `$request`/`$request_uri`，不记录 Referer。
- 新日志中的 query marker 为 0。
- HSTS、CSP、Permissions-Policy、`no-referrer`、`nosniff`、frame deny 在 origin 和公网均存在。
- `nginx -t`：PASS。

公网边缘结果：

- TLS 1.3 成功。
- TLS 1.2 仍成功协商 `ECDHE-ECDSA-CHACHA20-POLY1305`，FAIL。

时间结果：chrony 使用多个来源，`NTPSynchronized=yes`、Leap status Normal；最终系统时钟相对 NTP 慢约 42.6 微秒，远小于 2 秒门禁。

### 5.5 浏览器与响应式矩阵

Playwright 使用官方 v1.62.0 noble 镜像，执行 Chromium、Firefox、WebKit，宽度 320/375/768/1024/1440，路由 `/login`、`/register`、`/select-node`、`/admin/login`、`/account`、`/conflict`、`/`。

| 层级 | route checks | 非 2xx/3xx | overflow | error groups | 判定 |
|---|---:|---:|---:|---:|---|
| 直接 Nginx/origin | 105 | 0 | 0 | 0 | PASS |
| Cloudflare 公网 | 105 | 0 | 0 | 15 | FAIL（单一外部根因） |

公网 15 组错误全部是 Cloudflare 自动注入的 `static.cloudflareinsights.com/beacon.min.js` 被 `script-src 'self'` 拒绝；没有应用 pageerror、导航失败或布局溢出。这里不应为未使用的分析脚本放宽 CSP，应关闭 Cloudflare 自动注入。

匿名页面对 opaque session endpoint 的 401 是应用主动处理的预期探测，被记录为 expected warning，不算 JavaScript 异常。

### 5.6 镜像、漏洞与 SBOM

最终 Controller 镜像：

- Alpine 3.24.1 OS 层 High/Critical fixable：0。
- Go binary High/Critical fixable：0。
- `govulncheck ./...` 可达漏洞：0；另有 2 个 package/2 个 module finding，但业务没有调用路径。
- CycloneDX SBOM 已生成并保存。
- 干净 PostgreSQL 17 上启动最终镜像：52 migrations、health 204、TLS 1.3 成功、TLS 1.2 拒绝。

### 5.7 负载、备份和生产状态

静态 TLS 入口对 loopback Controller 执行 10,000 请求、并发 1,000：

- 10,000 complete，0 failed。
- 681.54 requests/s。
- P50 1,328 ms；P90 1,649 ms；P95 2,047 ms；P99 2,606 ms；max 3,081 ms。
- TLS 1.3 / `TLS_AES_128_GCM_SHA256`。

该数据只说明单机静态 TLS 入口没有请求失败；不代表 1,000 个独立登录、心跳或数据库写事务通过。

生产备份：

- 数据库 custom dump SHA-256：`84053efa5f28fe20d26bebdbb3bed983ab0d3f4fb978c5f352e4ed1fbe27d9ca`，manifest 校验通过。
- 在全新 PostgreSQL 17 容器 `pg_restore --no-owner --exit-on-error` 成功。
- 归档 TOC 593 项；恢复出备份时 51/51 migrations 和 3/3/3 演示记录。
- 临时恢复容器已删除；长期备份未删除。

生产最终状态为 52/52 migrations 和 0/0/0 核心记录。三个节点、三个用户和三个全局用户均为 `.invalid` 演示 seed，先在备份恢复副本验证清理事务，再在生产执行同一事务；业务真实数据没有被识别到。备份仍可恢复清理前状态。

## 6. 本轮发现并修复的问题

下表覆盖整个任务的源码问题。详细断言、日志和文件差异都在 `server` 分支与证据目录中。

| 编号 | 问题 / 根因 | 实施的修复 | 复验 |
|---|---|---|---|
| F-01 | SERIALIZABLE 竞争使 login handoff 返回 500 | 仅对 PostgreSQL 40001/40P01 做 context-aware、有上界的整事务重试 | 真实进程 E2E PASS |
| F-02 | 灾难写入已入库但保护投影竞争后未冻结 | protection 整事务使用同一重试；maintenance 不再吞最终错误 | 60 秒 fail-closed 收敛不放宽，E2E PASS |
| F-03 | `ListUsers` join 后列名不限定，生产持续 ambiguous column | 所有 legacy user 列加 `u.` 限定并加真实 PG 测试 | 最终部署日志 ambiguous=0 |
| F-04 | SillyTavern 首次冷启动超过 30 秒造成假失败 | readiness 上限有界调整到 90 秒 | 冷启动及全量 PASS |
| F-05 | AI 脱敏测试搜索随机两字符导致误报 | 改为敏感 key 和精确 JSON 值断言 | count=20 与全量 PASS |
| F-06 | 子进程 helper 在普通轮次产生 SKIP | 非 helper 场景直接 PASS，父崩溃矩阵仍执行 | 全量/race 0 skip |
| F-07 | Admin 空列表可能是 null 并使 React 空态失败 | Store 保证 `[]`，前端 `listOrEmpty` 规范化 | Web 25/25 PASS |
| F-08 | `/api/auth` 登录限流中间件覆盖非凭据入口 | 登录锁定只挂 register/user/admin login；全局限流保留 | HTTP/OAuth/注册 PASS |
| F-09 | Controller 端口与 Docker build context 暴露面过大 | 8443 绑定 loopback；`.dockerignore` 排除秘密/证据/SQL | Compose/镜像/端口复验 PASS |
| F-10 | Agent 重启后读取旧 heartbeat 产生 readiness 假阳性 | readiness 要求 `last_seen_at` 晚于本次启动且全局 ready | 长进程 E2E PASS |
| F-11 | Online SMTP STARTTLS 关闭证书验证 | 最低 TLS 1.2、启用 CA 校验、删除 SSLv3 cipher | 本机候选 lint/smoke/cold start PASS |
| F-12 | Controller 没有统一安全响应头 | 最外层 middleware 覆盖成功、错误和 404 | origin/公网头实际 PASS |
| F-13 | master-key recovery 解析边界无法隔离测试 | 拆分 CLI/解析，限制 zstd 内存窗口与 envelope 大小 | 正反边界矩阵 PASS |
| F-14 | AI provider 协议和后台边界覆盖不足 | 对 OpenAI/Anthropic/Gemini、超时、错误、breaker 建立真实 HTTP 测试 | `internal/ai` 与 race PASS |
| F-15 | relay 测试 harness 先开放密文、后写 receipt，因果顺序错误 | harness 对齐生产，先持久 transferring 再开放下载 | 原子场景 10/10 PASS |
| F-16 | Online Showdown/Transformer/Jimp 依赖链含 XSS/ReDoS/旧依赖 | 本机候选固定修复 commit，替换 deprecated fork，固定 ONNX/sharp | 本机 audit 0、53 tests；尚未推 Online |
| F-17 | Controller backup 空列表 SQL 缺空格形成 `node.idWHERE` | 固定 SQL 分隔并加真实 PG/HTTP 空库测试 | PASS |
| F-18 | conflict retry rotation 的 `attempt` 未限定 | 改为 `transfer.attempt+1` 并覆盖完整失败→重试→发布 | PASS |
| F-19 | 持续心跳使立即 SERIALIZABLE 重试同频耗尽 | 8 次 25 ms→750 ms context-aware 指数退避 | 双 Agent/双 Tavern 恢复 PASS |
| F-20 | AI ticker 测试在请求持久化前取消 | 等待 fake Store 确认持久化后再取消 | 30/30 与 race PASS |
| F-21 | JSON 审计参数只处于 variadic any，PG 无法推断类型 | `$6/$7` 显式 cast 为 bigint | 身份恢复幂等/冲突 round-trip PASS |
| F-22 | 1 ms ticker fake Store 不具备生产 dedup 语义 | 第二轮 builder 等待 context 取消，不伪造重复落库 | 30/30、race 5/5 PASS |
| F-23 | Controller 大型备份 payload 的 `io.Copy` 不响应取消 | 使用每次 Read 前检查 context 的有界 reader | 取消/stream/pg_dump 矩阵 PASS |
| F-24 | 退役任务从 retry_wait/blocked 恢复后最终 CAS 必然失败 | 完成 CAS 接受入口允许的恢复态，仍拒绝终态/非法态 | 真实事务、全量、race PASS |
| F-25 | 空活动租约快照编码为 JSON `null`，adapter 要求数组，撤销会永久 400 | nil slice 在发送前规范化为 `[]`，保持 fail-closed adapter 合同 | 新单测、216.26 秒聚焦 race、最终全量/race PASS |
| F-26 | takeover handler 测试把活动租约到期时间固定在当天 11:00 UTC，CI 晚于该时刻运行后夹具自然过期 | 到期时间改为测试执行时刻加一小时；只修测试墙钟，不改生产判断 | 普通 100/100、race 100/100、全包 short race PASS |

F-25 是最终完整 race 发现的真实产品问题：故障恢复期间 Controller 返回空权威租约集合，Go 的 nil slice 会编码为 `null`；Online adapter 合同只接受数组，所以 Agent 会持续重试 400，冲突恢复无法完全冻结。修复只做合同规范化，没有增加旁路、重试层或宽松解析。

此外，覆盖率脚本原来读取 `go tool cover` 的一位小数，79.95% 可能显示成 80.0% 而误过门禁；新的脚本直接读取 profile、严格校验格式、跨包去重、用整数 statements 计算，并有独立自测。

## 7. 已在当前生产服务器实施的变更

### 7.1 备份与回滚点

变更前创建 `/root/stcontrol-server-backups/20260824T0930Z`：

- PostgreSQL custom dump、globals、SHA-256 manifest。
- `.env`、Controller Compose 配置、TLS cert/key。
- Nginx 主配置与 site 配置。
- 配置、证书和数据库备份权限 0600。
- 旧 Controller 镜像标签 `stcontrol-controller:rollback-20260824T0930Z`。

数据库恢复已经实际执行，不是只运行 `pg_restore --list`。如需要回退应用，先确认数据库 schema 兼容，再把 rollback tag 重新标为部署 tag并重建 Controller；如需要回退数据库，必须先停写、另做当前状态备份，再向空库恢复，不能对当前库直接叠加 `pg_restore`。

### 7.2 Nginx 和敏感日志

生产 Nginx 已实施：入口 TLS 1.3、回源 TLS 1.3、安全 access format、HTTP redirect 丢弃 query，并通过 `nginx -t` 和实际握手复验。

旧 access log 已按潜在敏感日志处理：当前 `access.log` 被截断，历史轮转 access log 被 unlink；error log 保留。该删除是不可恢复的安全销毁，没有在备份中复制可能含 query 的旧 access log。当前两个 access log 的 query marker 均为 0。

仓库提供可复用模板：

- `deploy/nginx/http-hardening.conf.example`
- `deploy/nginx/stcontrol-site.conf.example`

### 7.3 时间、凭据、镜像和数据

- 安装并启用 chrony，多个来源 reach 正常，系统同步状态 yes。
- 当前生产数据库密码和 `CONTROLLER_SECRET_KEY` 已轮换，未写入仓库、报告或日志；`.env` 和 Compose 配置保持 0600。
- 最终镜像在干净上下文构建、扫描、生成 SBOM，并部署为当前运行镜像。
- 执行三次 Controller 重启和一次 PostgreSQL 重启；服务全部恢复。
- 在备份恢复副本验证后清理 demo seed；当前生产数据库是空业务状态。
- 测试 PostgreSQL、备份恢复临时容器和临时网络均已清理；最终只运行生产 Controller 和 PostgreSQL 两个容器。

## 8. 尚未关闭的问题与精确修改方法

### P0-01 Cloudflare 最低 TLS 版本

现状：origin/Nginx 已拒绝 TLS 1.2，但访问者先连接 Cloudflare，公网仍可协商 TLS 1.2。源站配置无法约束 CDN 边缘。

修改方法：

1. Cloudflare Dashboard → SSL/TLS → Edge Certificates。
2. 确认 TLS 1.3 开关为 On。
3. 把 **Minimum TLS Version** 改为 **TLS 1.3**。只开启 TLS 1.3 并不会禁用 TLS 1.2。
4. 等待边缘传播后执行：

   ```bash
   openssl s_client -brief -connect test.pixcora.com:443 -servername test.pixcora.com -tls1_2 </dev/null
   openssl s_client -brief -connect test.pixcora.com:443 -servername test.pixcora.com -tls1_3 </dev/null
   ```

5. 第一条必须以 protocol version/handshake failure 结束；第二条必须显示 TLSv1.3。

Cloudflare 官方文档：[Minimum TLS Version](https://developers.cloudflare.com/ssl/edge-certificates/additional-options/minimum-tls/)。

### P0-02 Cloudflare Web Analytics 注入

现状：普通 curl UA 未必看到注入，但 Chromium/Firefox/WebKit 都稳定看到 `static.cloudflareinsights.com/beacon.min.js`。应用 CSP 正确拒绝非同源脚本。

修改方法：

1. Cloudflare Dashboard → Web Analytics。
2. 找到 `test.pixcora.com` → Manage Site。
3. 把 Automatic setup 设为 **Disable**；不要仅关闭 SPA measurement。
4. 不要把 `static.cloudflareinsights.com` 加入 CSP，除非业务明确决定采集并完成隐私评审。
5. 重新执行 `scripts/browser-matrix.cjs`，要求 105 route check、0 route failure、0 overflow、0 error group。

Cloudflare 官方说明明确指出 proxied site 默认可自动注入，Manage Site 的 Disable 会停止注入：[Web Analytics setup](https://developers.cloudflare.com/web-analytics/get-started/)。

### P0-03 SillyTavern-online 远端依赖漏洞

现状：`origin/online` 固定 SHA 的 `npm audit --omit=dev` 为 56 个漏洞，其中 4 critical、16 high。本机 staged 候选改动 audit 为 0，并通过 53 个相关 Node tests、ESLint、真实 transformer 推理、图片解析和完整冷启动。

修改方法：

1. 在 `/root/Sillytarven-online` 审查 `git diff --cached`，确认 12 个候选文件符合该仓库发布策略。
2. 以独立分支提交并推送到 SillyTavern-online；不要把该 patch 伪装成 stcontrol 提交。
3. 在全新 clone 中执行：

   ```bash
   npm ci --ignore-scripts
   npm run postinstall
   npm audit --omit=dev
   node --test tests/stcontrol-adapter.node.test.mjs tests/stcontrol-agent-auth.node.test.mjs tests/stcontrol-disaster-login.node.test.mjs tests/stcontrol-heartbeat.node.test.mjs tests/stcontrol-native-entry-guards.node.test.mjs tests/stcontrol-routes.node.test.mjs tests/showdown-security.node.test.mjs tests/transformers-security.node.test.mjs
   ```

4. audit 必须为 0，全部 tests 必须 0 fail/0 skip，然后更新 stcontrol CI 固定的 Online SHA，而不是长期跟踪可变 branch head。

候选完整 patch：`server-test-evidence/20260824T005513Z/online-final-working-tree.patch`。

### P0-04 Git 历史 secret 候选

现状：当前生产 secret 已轮换，当前运行风险已关闭；历史 `run-live-controller.bat` 仍包含一个 44 字节 secret 形态值。三个其他 finding 是测试 fixture。

修改方法：

1. 确认所有曾可能使用历史值的 Controller、见证、恢复和备份环境均已轮换。
2. 把脚本改为只读取环境变量或 0600 文件，不保留默认值。
3. 先制作 repository mirror 备份并通知所有协作者停写。
4. 用 `git filter-repo --replace-text` 对精确历史值做替换；不要用宽泛正则误删正常 base64 fixture。
5. 审查所有 branches/tags 后，协调 force-push；所有 clone 必须重新 clone 或按书面步骤清除旧对象。
6. 对全部历史重新运行 gitleaks，未处置真实 finding 必须为 0。

本轮不强行执行历史重写，因为它是不可兼容的远端操作，GitHub PAT 不能代替所有协作者授权。

### P1-01 真实多机、网络分区和容量

最低补测拓扑：CTRL-A、CTRL-B、PG-A、C1/C2/C3、S1/S2、独立 LOAD。必须用生成数据执行：

- `tc netem` 延迟、丢包、抖动、乱序和完全分区。
- owner Agent 整机掉线、三节点见证、A/B 分叉、被动 Controller 接管和 generation fence。
- 跨 NAT TLS hostname/CA/过期证书矩阵。
- 1,000 个独立账号/会话的登录和 heartbeat，而不是同一静态 URL 的 1,000 并发。
- 大文件数据面饱和时同时记录 login、heartbeat、admin、ACK 的 P50/P95/P99 和错误率。

合格标准必须直接沿用 `服务器测试清单.md`，不能用本轮单机静态压测替代。

### P1-02 真实认证浏览器旅程

需要专用验收账号和 provider sandbox，不能使用未来真实用户数据。覆盖 Chromium/Firefox/WebKit 与至少 320/768/1440 px：注册、邀请码错误/过期、丢响应幂等、登录短码单次消费、管理员权限降级、账号导入、LinuxDo/Discord state 绑定/回放/guild 错误、密码/OAuth 离线收敛、冲突恢复 UI。保存截图、HAR（先脱敏）、console 和数据库最终事实。

### P1-03 宿主防火墙 default-deny

现状：只有 22/80/443 对外监听，8443 只在 127.0.0.1；有效暴露面符合预期，但 INPUT policy 是 accept。

实施前必须先确定 SSH 管理来源/救援控制台，并备份 `iptables-save`/`ip6tables-save`。规则至少允许 loopback、established/related、必要 ICMP/ICMPv6、SSH 管理来源、80/443，然后 default drop；同时保留 Docker 和 fail2ban 链。先设置自动回滚计时器，再从第二个会话验证 SSH 和公网 health，最后持久化。由于当前没有管理来源 allowlist 和带外控制台信息，本轮没有冒险修改生产 INPUT policy。

### P1-04 上一版本滚动升级

需要固定“上一正式发布镜像”和 migration 支持范围，按旧 Controller→新 Controller、旧 Agent→新 Agent、PostgreSQL 重启/切换执行，验证旧/新协议拒绝策略、generation、未完成 workflow、回滚可行性。当前完成的三次同版本 Controller 重启不能替代版本升级。

## 9. CI、提交与远端复核

新增 `.github/workflows/server-gates.yml`：

- `static-and-short-race`：mod verify、vet、build、coverage parser 自测、short race、Web test/build/audit。
- `full-postgresql-coverage`：PostgreSQL 17 service、固定 Online clone、显式 postinstall、真实 pg_dump 17、完整 PostgreSQL coverage、精确 80% 门禁、完整 PostgreSQL race、coverage artifact。
- full job 仅对 `server` 分支、以 server 为 base 的 PR 或手动触发执行；并发取消旧 run。

本地 actionlint 1.7.12 已通过。最终提交推送后必须等待两个 job 全部 success，并记录：

首次新增工作流运行时，`static-and-short-race` 在 `writer lease active` 子测试暴露了 F-26；该失败已复现并修复，不能被重跑掩盖。修复提交会触发新的 run，旧 run 按 concurrency 取消，只有新 run 的两个 job 全绿才算 CI 关闭。

```bash
git fetch origin server
git rev-parse HEAD
git rev-parse origin/server
git status --short --branch
```

HEAD 与 `origin/server` 必须相同；工作区只允许保留明确排除的两个用户 SQL 文件。GitHub 凭据由用户在会话中提供，只临时用于本次 push，不写入 remote URL、credential helper、配置、报告或日志；推送完成后应在 GitHub 中撤销/轮换该 PAT。

## 10. 发布签署

当前可签署的范围是：**stcontrol 源码门禁、最终 Controller 镜像、当前单机生产源站和备份恢复已通过；整个清单不能签 PASS。**

只有在 P0-01 至 P0-04 关闭，并在目标真实拓扑完成 P1-01、P1-02、P1-04（P1-03 按明确网络策略落地）后，重新执行清单且所有必测项有证据，最终结论才能改成 PASS。72 小时稳定性测试保持本轮用户豁免状态，不倒签为通过。
