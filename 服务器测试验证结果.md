# stcontrol / SillyTavern-online 服务器测试验证结果

## 1. 结论

验收结论：**FAIL — 当前版本不得直接发布到生产环境。**

本轮已经把 stcontrol 的 `flash` 同步到 GitHub 最新提交，完成了真实 PostgreSQL、真实 `pg_dump`、Go race detector、Linux 原子发布/磁盘满/进程崩溃、跨仓 adapter、完整 SillyTavern 冷启动、Web、镜像构建、TLS、日志、秘密、依赖漏洞和部分负载测试。功能自动化在本轮修复后全部通过，且最终全量 PostgreSQL 回归为 0 fail / 0 skip。

但是，以下发布硬门禁仍未满足：

1. 清单要求的全仓覆盖率至少 80%。修复后的权威全包严格轮次已 0 fail，精确结果为 **74.7%**（`13,263/17,756` statements），仍缺 **942** 条已执行 statement 才能达到 80%。
2. Git 历史存在一个无占位标记的 44 字节 Controller secret 形态值；虽然不等于当前线上密钥，仍必须按历史泄露处置。
3. Nginx 和 Cloudflare 公网入口实际接受 TLS 1.2，不符合本清单“仅 TLS 1.3”的要求。
4. Nginx 使用默认 access log，已保存 887 条带 query 的请求；参数名统计中出现 `username`、`psd`，不符合“URL/query/access log 不含秘密”。
5. 系统显示 `System clock synchronized: no`，不满足节点时钟同步前置条件。
6. 真实多服务器/NAT/网络分区、1,000 并发登录与心跳、三浏览器矩阵、72 小时 soak 和上一版本滚动升级没有合格环境证据。
7. 线上运行容器仍是旧二进制，并非本次最新源码和修复后的镜像；本轮应用安全响应头等修复也尚未部署到公网边缘。

因此，本报告不能签署“生产可用”。第 11 节给出了逐项修复和复验条件；只有所有 P0/P1 阻断关闭，并重新完整执行清单后，才能发布。

## 2. 验收范围与固定版本

验收时间：2026-08-24，时区 Asia/Shanghai。

| 项目 | 固定值 | 最终远端复核 |
|---|---|---|
| stcontrol 验收基线 | `flash` | 修改前 `HEAD == origin/flash` |
| stcontrol 基线 SHA | `2f1ee68e6641d5d4b50bcca96bc55ac1a7bef93b` | 修改前 ahead/behind `0/0` |
| stcontrol 交付分支 | `server` | 本报告、测试证据与本轮 stcontrol 修复均交付至远端该分支 |
| SillyTavern-online 分支 | `online` | `HEAD == origin/online` |
| SillyTavern-online 基线 SHA | `b4dd65b47c178ea87ea42f56ac1b7ef1a8a934cd` | ahead/behind `0/0`；其上有本轮未提交安全修复 |
| PostgreSQL 测试服务 | PostgreSQL 17.10，独立容器、独立数据库 | 未复用生产数据库 |
| 主机 | Debian 13.6，Linux 7.1.8 x86_64 | 4 CPU，约 5.8 GiB RAM |
| 验收工具 | Go 1.26.7 / Node 20.20.2 / npm 10.8.2 | 工具可用 |
| Controller 构建/运行镜像 | Go 1.26.7 / Node 24 / Alpine 3.24.1 | 三个基础镜像均固定 patch/tag 与 digest |

最终 SHA 证据：`server-test-evidence/20260824T005513Z/final-remote-sha-verification.txt`。

同步前存在本地验收改动，已先保存为安全 stash，再 fast-forward 到远端 `flash`，随后语义合并。安全备份仍保留为：

- `stash@{0}: preserve server acceptance work before updating origin/flash 2026-08-24`
- 原有 `stash@{1}` 未改动。

本轮 stcontrol 源码、测试、清单、报告及可安全公开的脱敏证据已提交并推送到远端 `server` 分支。用户工作区中与本次验收无关的 `scripts/seed-demo.sql`、`scripts/clear-demo.sql` 未纳入提交。SillyTavern-online 的跨仓候选修复未在该仓库 commit/push；仅把脱敏 working-tree patch 作为本仓库验收证据保存。线上容器未重启、未替换、未执行写入型管理操作，因此“已推送候选”不等于“已部署生产”。

## 3. 隔离与证据

证据根目录：`server-test-evidence/20260824T005513Z/`，当前共 234 个证据文件，清单见 `evidence-index.txt`。

隔离措施：

- 测试 PostgreSQL：`stcontrol-acceptance-pg-20260824`，仅绑定 `127.0.0.1:55432`，使用可丢弃测试数据。
- 生产 PostgreSQL `stcontrol-db-1` 未暴露宿主端口，测试套件未连接该库。
- 生产 Controller 仅做 HTTPS、证书、日志、进程、镜像哈希和数据库健康的只读检查。
- 跨仓测试先使用 `/root/Sillytarven-online` 的固定 SHA 建立基线；发现依赖/ESLint 问题后，在该仓库工作区加入未提交修复并完成回归，未 push、未部署。
- 测试 DSN 和生成测试凭据没有写入本报告；秘密扫描报告使用 100% redaction。

## 4. 总体结果矩阵

| 类别 | 结果 | 核心结论 | 主要证据 |
|---|---|---|---|
| 仓库/构建/基础测试 | PASS（已执行部分） | Go/Web/adapter 测试、相关 ESLint 与镜像构建通过 | `go-1.26.7-static.log`、`web-test-upgraded.log`、`online-stcontrol-tests-after-eslint.log`、`online-stcontrol-eslint-fixed.log` |
| PostgreSQL/迁移 | PASS（测试环境） | 已记录轮次 901 RUN、0 fail、0 skip；最终权威严格轮次所有包 PASS；真实 pg_dump 通过 | `go-full-postgres-final-zero-skip.log`、`go-server-final-authoritative.log` |
| Linux/Race/覆盖率 | FAIL | 完整 short race、真实 PostgreSQL Controller 聚焦 race 与 main 生命周期 race 均通过；权威严格覆盖率 74.7% < 80% | `go-race-short-server-final.log`、`go-race-controller-targeted-server-final.log`、`coverage-gate-server-final.log` |
| TLS/控制通道 | FAIL | Origin 为 TLS 1.3；Nginx/Cloudflare 可协商 TLS 1.2 | `tls13-handshake.txt`、`tls12-rejection.txt`、`nginx-tls12-handshake.txt`、`public-edge-tls12.txt` |
| 身份/浏览器 | BLOCKED | HTTP/身份自动化通过；无三浏览器、真实 OAuth 和响应式矩阵 | `go-full-postgres-final-zero-skip.log`、`web-test-final-upgraded.log` |
| 快照/恢复/冲突 | PASS（自动化） | 原子发布、ENOSPC、大归档、恢复、冲突、data-fault 全绿 | `linux-agent-filesystem.log`、`go-full-postgres-final-zero-skip.log` |
| 灾难/分区/恢复 | PARTIAL | 真实本机多进程恢复通过；无真实三服务器网络分区/NAT | `process-e2e-fresh-heartbeat-fix.log`、`go-full-postgres-final-zero-skip.log` |
| 容量/性能 | PARTIAL FAIL | 10k 候选、1k 在线事实通过；1k 静态 HTTPS 通过；未做 1k 登录/心跳 | `go-full-postgres-final-zero-skip.log`、`ab-https-static-c1000-n10000.log` |
| 安全/隐私 | FAIL | 三个生产依赖审计均已清零；仍有历史 secret、query 日志、TLS 1.2，且应用安全头尚未部署到公网 | `trivy-controller-go1267.log`、`govulncheck-go1267.log`、`gitleaks-findings-safe.json`、`zz-final-hardening-round-summary.txt` |
| 长稳/升级 | BLOCKED | 未完成 72 小时 soak、三轮滚动重启和上一版本升级 | 无合格证据 |

## 5. 已通过的测试

### 5.1 源码、构建与单元测试

最终结果：

- `go mod verify`：PASS。
- `go vet ./...`：PASS。
- `go build ./...`：PASS。
- `go test -short -count=1 ./...`：PASS。
- Web `npm test`：4 个文件、25 个测试全部 PASS。
- Web `npm run build`：PASS；Vite 生产产物成功生成。
- Docker Compose `config --quiet`：PASS。
- Controller 镜像从干净上下文构建：PASS。
- 两仓库 `git fsck --no-dangling`：PASS。
- 非 Markdown 源码冲突标记扫描：PASS。

证据：

- `go-vet-server-final.log`、`go-build-server-final.log`
- `web-test-server-final.log`、`web-build-server-final.log`、`web-audit-server-final.log`
- `docker-compose-config-server-final.log`、`docker-build-final-secure.log`
- `git-fsck.txt`、`conflict-marker-scan.txt`

### 5.2 SillyTavern-online adapter

指定六套 adapter 测试仍为 **47 pass、0 fail、0 skip**；加入 Showdown 与 Transformers 安全回归后，同一最终 Node 轮次合计 **53 pass、0 fail、0 skip**。

覆盖了 adapter 路由、Agent 签名认证、灾难登录、心跳、原生入口保护和 stcontrol 路由。跨仓 Go E2E 还分别调用了真实 adapter 和完整 SillyTavern server。

证据：`online-stcontrol-tests-final-secure-deps.log`、`go-full-postgres-final-zero-skip.log`、`zz-final-hardening-round-summary.txt`。

相关 12 文件 ESLint 为 0 error；`npm ci --ignore-scripts` 可从 lockfile 干净复现；生产依赖审计已为 0，详见第 8 节。

### 5.3 真实 PostgreSQL 全量测试

最终命令使用 `-p 1 -count=1 -v ./...`，并设置独立 PostgreSQL DSN、真实 `pg_dump` 路径和固定 SillyTavern 根目录。

最终日志统计：

- `=== RUN` 事件 901 个。
- 0 `FAIL`。
- 0 `SKIP`。
- 0 panic。
- 0 race marker。
- 退出码 0。

关键实测：

- 迁移从 0001 到最新版本连续应用。
- 真实 `pg_dump` 灾备与 master-key 恢复演练通过。
- 并发 writer lease、handoff 单次消费、Controller generation fence 通过。
- HTTP 管理员、Agent、OAuth、注册、账号导入、密码/OAuth 收敛通过。
- snapshot、restore、conflict、replica cleanup/integrity、retirement、data-fault 状态机通过。
- 10,000 个导入候选持久建立并分页，无遗漏；最终一轮耗时约 44.73 秒。
- 1,000 在线事实下容量拒绝路径通过。

证据：`go-full-postgres-final-zero-skip.log`。

新增安全/边界测试后，又以相同真实 PostgreSQL、真实 `pg_dump`、完整 Online、`-p 1 -count=1 -coverpkg=./... -covermode=atomic` 完整执行一轮：所有包 PASS。该轮曾首次暴露 relay 验收 harness 的因果时序竞态；修正 harness 使其与生产 Agent 顺序一致后，失败子场景连续 10/10 通过。最终在身份恢复 SQL 修复和 AI ticker 测试模型修正后，再执行一轮带 `pipefail` 的权威全包命令，命令退出 0：Agent 235.956 秒、Controller 107.898 秒、Store 84.241 秒，其余包也全部通过。详见 F-15、F-21、F-22 和 `go-server-final-authoritative.log`。

### 5.4 Race、Linux 文件系统与真实进程

`go test -race -p 1 -count=1 ./...`：所有包 PASS，0 race report，退出码 0。

本轮新增测试与安全修复后的最终 race 复验也全部通过：`go test -race ./... -short -p=1 -count=1` 全包 PASS；身份恢复、OAuth、节点恢复投影、灾备扫描等真实 PostgreSQL Controller 聚焦矩阵 PASS；Controller main 启停生命周期聚焦 race PASS；AI 1 ms ticker 聚焦 race 连续 5/5 PASS。

Race 下主要包耗时：

- Agent：253.194 秒。
- Controller：199.309 秒。
- Store：69.646 秒。

Linux/进程矩阵通过：

- `RENAME_EXCHANGE` 三个崩溃边界均恢复。
- 真实内核 `ENOSPC` 传播通过。
- 大归档发布没有 partial replica。
- source/target Agent 进程崩溃后恢复。
- Controller 与 SillyTavern 进程重启后恢复。
- Controller–双 Agent–双 SillyTavern 灾难链路最终耗时 214.07 秒并通过。
- 完整 SillyTavern 冷启动 + CSRF E2E 最终耗时 10.64 秒并通过；独立最终复验为 13.28 秒。

证据：`go-race-postgres-final-go1267.log`、`go-race-short-server-final.log`、`go-race-controller-targeted-server-final.log`、`go-race-controller-main-server-final.log`、`go-ai-ticker-race-5x-server-final.log`、`linux-agent-filesystem.log`、`process-e2e-fresh-heartbeat-fix.log`。

### 5.5 TLS、权限与端口中已通过的部分

- Controller origin `127.0.0.1:8443` 成功协商 TLS 1.3：`TLS_AES_128_GCM_SHA256`。
- Controller origin 明确拒绝 TLS 1.2。
- 直接连接 origin 使用系统默认 CA 时校验失败，符合 Cloudflare Origin CA 不应直接被公网客户端信任的模型。
- 证书 SAN 为 `*.pixcora.com`、`pixcora.com`，有效期 2025-11-12 至 2040-11-08。
- `controller.docker.yaml`、TLS 私钥和 `.env` 权限为 0600；证书为 0644。
- Controller 和测试 PostgreSQL 均只绑定 loopback；生产 PostgreSQL 未发布宿主端口。
- 当前监听端口为 SSH 22、Nginx 80/443、loopback Controller 8443、loopback 测试 PostgreSQL 55432，没有额外 Agent 数据面暴露。

证据：`tls13-handshake.txt`、`tls12-rejection.txt`、`controller-certificate.txt`、`local-sensitive-permissions.txt`、`host-time-ports-containers.txt`。

### 5.6 部分负载基线

对 loopback Controller 静态 HTTPS 首页执行 1,000 并发、10,000 请求：

- TLS 1.3。
- 10,000 complete，0 failed。
- 5,766.02 requests/s。
- P50 70 ms，P90 561 ms，P95 1,027 ms，P99 1,146 ms，max 1,298 ms。
- Controller 内存从约 18.34 MiB 上升到 65.7 MiB；进程继续正常服务。

证据：`ab-https-static-c1000-n10000.log`、`load-static-before.txt`、`load-static-after.txt`。

该结果只证明静态 TLS 入口的部分承压能力，**不能替代**清单要求的 1,000 并发登录/心跳、数据库事务成功率、分布式来源 IP 和控制/数据面隔离压测。

## 6. 本轮发现并已修复的问题

以下改动均已纳入 stcontrol 的 `server` 分支；尚未部署到线上环境。

### F-01 SERIALIZABLE 事务竞争导致登录交接 500

现象：首轮真实进程 E2E 在心跳并发时，登录交接约 195 秒后返回 HTTP 500“登录交接创建失败”。

根因：`CreateLoginHandoff` 的 SERIALIZABLE 事务与心跳/租约更新竞争，PostgreSQL `40001` 没有重试。

修复：新增统一 `retrySerializable`，只对 PostgreSQL `40001` 和 `40P01` 做最多 8 次、有 context 取消约束的整事务重试；重试使用 25 ms 起步、750 ms 封顶的指数退避，总等待仍有明确上界；应用到登录交接、活动租约获取、snapshot workflow progress 和保护状态投影。

验证：事务重试单测全部通过；真实进程 E2E 连续多轮越过原失败点并最终 PASS。

涉及文件：

- `internal/store/transaction_retry.go`
- `internal/store/transaction_retry_test.go`
- `internal/store/handoffs.go`
- `internal/store/leases.go`
- `internal/store/snapshots.go`

### F-02 灾难写入事实已入库但用户没有冻结

现象：Controller 恢复后，独立写入 reconciliation 已为 `conflict`，但 global/legacy identity、lease、protection 和 `replica_conflicts` 仍未冻结。

根因：安全保护投影也是 SERIALIZABLE 事务，竞争错误没有重试；`runNodeMaintenance` 还把最终错误静默丢弃。

修复：为整笔 `ReconcileProtectionStates` 增加同一有界重试，并在非 shutdown 错误时记录日志。

验证：短退避版本在持续双节点心跳下仍可重复耗尽；扩展为 8 次有界退避后，原始 60 秒收敛门禁未放宽，聚焦真实进程 E2E 215.80 秒 PASS，确认 identity/lease/protection/conflict 全部持久冻结且控制面保持 fail-closed。

涉及文件：`internal/store/protection.go`、`internal/controller/agent.go`。

### F-03 生产 `ListUsers` 查询持续报 ambiguous column

现象：生产 PostgreSQL 日志最近 10 分钟统计到 20 次 `column reference "status" is ambiguous`；历史日志反复出现同一错误。

根因：`users u LEFT JOIN global_users gu` 后，`status`、`created_at` 等列未使用 `u.` 限定。

修复：将所有 legacy user 投影列明确限定为 `u.auth_provider`、`u.oauth_id`、`u.avatar_url`、`u.email`、`u.home_node_id`、`u.status`、`u.created_at`。

验证：新增真实 PostgreSQL 子测试 `legacy admin user list qualifies joined columns`；聚焦回归和最终全量均 PASS。

涉及文件：`internal/store/users.go`、`internal/store/postgres_integration_test.go`。

注意：线上仍运行旧镜像，此错误只有在安全升级并部署新镜像后才会停止。部署后必须确认连续 10 分钟该错误计数为 0。

### F-04 完整 SillyTavern 冷启动 30 秒误判失败

现象：首次启动会生成缩略图并编译前端，实际约 30–31 秒；原 30 秒 readiness deadline 偶发超时。

修复：把冷启动 readiness 上限改为有界 90 秒，不改变请求级超时和业务断言。

验证：聚焦用例、覆盖率轮次和最终全量均通过；稳定安全依赖后的冷启动耗时 10.64–13.28 秒。

### F-05 AI 脱敏测试随机误报

现象：测试搜索任意两字符子串 `fp`，随机伪匿名引用偶尔包含该组合，导致假失败。

修复：改为检查敏感 JSON key `compatibility_fingerprint` 和精确 JSON 值 `"fp"`，仍保留真实泄露断言。

验证：单测 `-count=20` 和最终全量通过。

### F-06 helper 入口造成验收日志 SKIP

现象：两个只供子进程调用的 helper 测试在正常进程中 `t.Skip`，违反清单 0 skip。

修复：非 helper 环境直接返回 PASS；真正崩溃矩阵仍由父测试设置环境并调用。

验证：最终全量和 race 均 0 skip，父崩溃矩阵正常执行。

### F-07 Admin API 空列表导致 React 空态崩溃风险

修复：Store 空列表统一验证为 JSON `[]`；前端用 `listOrEmpty` 规范化 nodes、links、users、backups、alerts、admins 的 nullish 响应，并增加测试。

验证：Web 25/25 PASS，生产构建 PASS，Go Store 测试 PASS。

### F-08 `/api/auth` 中间件范围过宽

修复：全局 API rate limit 保留在 auth subtree；登录限流/用户名锁定仅挂载到 register、user login、admin login，避免 registration status/OAuth 等非凭据入口被错误套用登录锁定。

验证：Controller HTTP、rate limit、OAuth、注册与最终全量测试通过。

### F-09 容器暴露和构建上下文

修复：Compose Controller 端口改为 `127.0.0.1:8443:8443`；新增 `.dockerignore`，排除 `.env`、私钥/证书、生产配置、Git、node_modules、dump 和 coverage，避免把秘密或大文件送入构建上下文。

验证：Compose config PASS；干净镜像构建 PASS；实际端口为 loopback。

### F-10 多进程重启后的 readiness 假阳性

现象：重启两台真实 Agent 后，测试只检查数据库里上一次 heartbeat 留下的 `online/compatible` 状态，可能在新进程尚未发送 heartbeat 时提前继续；紧接着注册会被正确处于恢复对账状态的 Controller 以 503 fail-closed 拒绝。

修复：`waitForProcessE2ENodeReady` 增加可选 `seenAfter` 边界；每次启动/重启 Agent 前记录 UTC 时间，只有 `nodes.last_seen_at` 严格晚于该启动时间，且节点 active/compatible、adapter telemetry 和全局 control-plane readiness 同时满足，才允许继续。

验证：精确多进程长测在修复后 214.60 秒通过；最终全量回归再次执行此路径。

### F-11 SMTP STARTTLS 绕过证书校验

现象：`src/email-service.js` 对 587/STARTTLS 显式设置 `rejectUnauthorized: false`，并配置过时的 `SSLv3` cipher 字符串；攻击者可利用错误/伪造证书进行中间人攻击。

修复：删除 `SSLv3` cipher 配置，把最低版本固定为 TLS 1.2，并设置 `rejectUnauthorized: true`。私有 SMTP 若使用企业 CA，应把 CA 安装到 Node/容器 trust store，禁止以关闭校验作为兼容手段。

验证：相关 ESLint 0 error；Nodemailer 9 `createTransport/sendMail` JSON transport API smoke 通过；完整 SillyTavern 冷启动复验 11.04 秒通过。

### F-12 Controller 缺少统一应用安全响应头

修复：在 Router 最外层加入统一中间件，对成功、业务错误和 404 响应均设置 HSTS、CSP、`nosniff`、frame deny、`no-referrer` 和最小 Permissions-Policy。CSP 的 script 只允许同源且不允许 inline；当前 React UI 仍使用 style attribute，因此仅 CSS 暂时保留受限的 `'unsafe-inline'`。

验证：新增测试逐一断言成功/错误/404 的所有 header，且 CSP 不含 `script-src 'unsafe-inline'`；完整 PostgreSQL、race、vet/build 均通过。该改动尚未部署，公网 header 仍按 S-06 判定失败。

### F-13 master-key recovery 解析边界不可单测

修复：把 CLI 的环境读取/退出与核心归档解析拆开；核心函数对 zstd decoder 的内存和窗口各限制 256 MiB，只接受精确文件名 `master_key_recovery.json` 的普通文件，envelope 上限 1 MiB，并返回分层错误而不是直接终止进程。

验证：真实加密 envelope round-trip 通过；错误口令、缺文件、空文件、目录、无效/截断 zstd、零/超大/malformed envelope 和无效 envelope 全部被拒绝。

### F-14 AI provider 与后台边界覆盖不足

修复：为 OpenAI Chat/Responses、Anthropic、Gemini 使用真实 `httptest` server 验证协议、认证 header、响应解析、超时、无效 URL/JSON、4 KiB 有界错误；Gemini key 明确不进入 URL。另覆盖全部任务 prompt、ticker/cancel、catalog、breaker half-open/reset 和 observation builder 边界。

验证：`internal/ai` 严格聚合覆盖率提升到 86.8%；完整 PostgreSQL 和 race 通过。

### F-15 relay 验收 harness 因果顺序竞态

现象：首次新增覆盖率全量轮次中，`relay publish survives lost source result` 偶发在目标 `verifying` 时报告 workflow state conflict。

根因：测试 harness 先把 relay ciphertext 标记为 stored，再持久化源端 `transferring` receipt；目标 worker 可在两者之间抢跑。生产 Agent 本来先上报 `transferring` 再上传密文，缺陷只在验收模型中。

修复：harness 对齐生产因果顺序，先持久化 `transferring`，再开放 ciphertext 下载。

验证：原失败子场景连续 10/10 PASS；随后真实 PostgreSQL 严格覆盖率全量所有包 PASS。

### F-16 SillyTavern Markdown/Transformer 依赖链

修复：Showdown 固定到包含已知 metadata/table XSS 和 ReDoS 修复的上游 commit，并由 lockfile integrity 约束；加入三类公开 payload 和现有 SillyTavern extension 兼容测试。废弃且 2.17.2 被标记 broken 的 `sillytavern-transformers` 被替换为上游 `@xenova/transformers` 2.17.2；ONNX 统一为已验证的 1.16.1，sharp 固定到修复 2026 libvips 公告的 0.35.0，因此运行时最低版本明确提升为 Node 20.9。

验证：干净 `npm ci` 可复现；53/53 Node 测试和相关 ESLint 通过；真实 MiniLM 输出 `[1,384]` 且全为 finite；RGBA 通过应用 `getRawImage` 得到 2×3×4；完整 SillyTavern 冷启动 E2E 通过；`npm audit --omit=dev` 为 0。

### F-17 Controller 灾备空列表 SQL 拼接错误

现象：真实 PostgreSQL 空库访问 `/api/admin/controller-backups` 返回 500。

根因：动态拼接 `JOIN nodes ...` 与 `WHERE` 时缺少分隔空格，实际 SQL 形成 `node.idWHERE`。sqlmock 只匹配片段，未能发现真实解析错误。

修复：在固定 SQL 尾部加入明确空格；新增 Store 真实 PostgreSQL 回归，覆盖空列表、state filter、limit 和 before cursor；Admin HTTP 真实 PostgreSQL 集成同时断言空库成功。

验证：Store 聚焦回归和 Admin HTTP 聚焦回归均 PASS。

### F-18 冲突恢复重试的 capability rotation SQL 歧义

现象：把已排队的冲突处理安全标记失败后，经认证 retry API 重新启动，执行器在旋转传输 capability 时返回 `column reference "attempt" is ambiguous`。

根因：`UPDATE conflict_resolution_transfers ... FROM ... workflows` 同时可见 transfer/workflow 的 `attempt`，赋值使用了未限定的 `attempt=attempt+1`。

修复：明确改为 `attempt=transfer.attempt+1`；扩展真实 PostgreSQL 冲突验收，覆盖用户冲突摘要、分页差异、状态查询、注入失败、认证 retry、重新调度及最终发布。

验证：完整冲突证据/重试/发布聚焦链路 6.86 秒 PASS。

### F-19 持续心跳下 SERIALIZABLE 重试同频耗尽

现象：最新严格全量和随后一次隔离进程复验中，Controller 每轮保护状态投影的 5 次立即重试都与两台 Agent 的一秒心跳写入再次碰撞，60 秒内无法建立冻结事实。

根因：原重试虽有次数上界，但没有等待窗口；同一批写事务尚未结束时立即重放复杂事务，容易连续获得 `40001`。

修复：将安全重试扩展为 8 次 context-aware 指数退避（25/50/100/200/400/750/750 ms）。永久错误仍只执行一次，取消仍立即终止。

验证：重试单测 PASS；AI/Store/Agent 相关 race PASS；真实 Controller–双 Agent–双 SillyTavern 灾难恢复链路在不放宽 60 秒最终收敛门禁的情况下 215.80 秒 PASS。

### F-20 AI ticker 测试取消早于持久化

现象：全量复验中 builder 发出“已调用”信号后立即 cancel，ticker goroutine 偶发在 `InsertAIAdvisoryRequest` 前收到取消，断言看到空 requests。

修复：测试先有界等待 fake Store 确认一条正确 dedup 请求已经持久化，再取消并断言 worker 退出；生产 Supervisor 语义不变。

验证：聚焦用例连续 30/30 PASS，race PASS。

### F-21 身份恢复审计 JSON 参数类型无法推断

现象：真实 PostgreSQL HTTP 集成调用管理员密码身份恢复接口时返回 500，日志为 `could not determine data type of parameter $6`；因此创建、幂等回放和冲突回放的业务断言均无法继续。

根因：审计 INSERT 把 `password_version` 与 `staged_node_count` 仅作为 `jsonb_build_object` 的 variadic `any` 参数传入。PostgreSQL 无法从该上下文推断两个占位符的具体类型，而普通 mock 不会执行服务端类型推断。

修复：在 SQL 中把 `$6`、`$7` 分别显式转换为 `bigint`，保持 Go 参数和数据库字段的既有 64 位整数语义，不增加额外兼容分支。

验证：新增真实 PostgreSQL HTTP round-trip，创建恢复请求成功；同一 idempotency key 与相同 payload 返回同一结果；相同 key 配不同 payload 被拒绝。最终权威全包与 Controller 聚焦 race 均 PASS。

### F-22 1 ms AI ticker 测试替身不具备生产去重语义

现象：F-20 修正后，1 ms ticker 在极快测试机器上可能在取消前进入第二轮，内存 fake Store 因没有生产数据库的 dedup constraint 而保存两条以上请求，导致断言偶发失败。

根因：测试替身没有复现生产 Store 的去重约束；这是测试模型问题，不是 Supervisor 生产逻辑重复持久化缺陷。

修复：第一轮正常构建并落库；若出现第二次 builder 调用，则让它等待 `ctx.Done()` 并返回取消错误。这样测试仍验证“ticker 会执行、请求会持久化、取消会终止”，同时不会伪造不存在于生产数据库的重复行为。生产代码未改动。

验证：聚焦用例 30/30 PASS，race 5/5 PASS；随后带 `pipefail` 的最终全包权威命令退出 0。

## 7. 未通过：覆盖率门禁

修复 F-19～F-22 后，以真实 PostgreSQL、真实 `pg_dump`、完整 Online、`-p 1 -count=1 -coverpkg=./... -covermode=atomic` 重新执行带 `pipefail` 的权威全包轮次，所有包 PASS，命令退出 0。对 Go 多包 profile 的重复 block 按源码区间取最大执行计数后，精确结果为 **13,263 / 17,756 statements = 74.695877%**（`go tool cover` 显示 74.7%）。低于 80% 约 5.3 个百分点，至少还需执行 **942** 条当前未覆盖 statement；覆盖率脚本正确退出 1，所以发布判定仍为 FAIL。

| 权威指标 | 结果 |
|---|---:|
| 已覆盖 statements | 13,263 |
| 总 statements | 17,756 |
| 精确覆盖率 | 74.695877% |
| 发布门禁 | 80.0% |
| 距离门禁仍需覆盖 | 942 statements |

优先补测区域：

- Agent：heartbeat 主循环、adapter ownership HTTP handlers、controller backup handler、冲突证据传输、relay 拉取/恢复失败路径。
- Controller：后台 scheduler/reconciler 的取消、重试、终态、日志和启动/关闭路径。
- Store：conflict resolution、restore、snapshot、node retirement、relay、registration 的失败/回放/租约丢失分支。
- AI 已越过 80%；后续重点只需保持 provider/supervisor 回归，不应以删除覆盖代码降低分母。
- `cmd/agent`、`cmd/controller`：启动参数、配置失败、信号关闭和 CLI wrapper；master-key 核心解析已达 94.9%，未覆盖主要在进程边界。

精确修复要求：

1. 在 CI 固定执行清单原覆盖率命令。
2. 从 `coverage-server-final-authoritative-func.txt` 的 0% 和低于 60% 状态机开始补“有断言”的测试。
3. 已加入 `scripts/check-go-coverage.sh` 读取 `go tool cover -func` 的 `total`；本轮用 80.0 实测明确非零失败。仍需接入 CI 必跑 job。
4. 达到 80 后再次执行 full PostgreSQL、race、Linux 进程矩阵，不能只执行新增单测。

证据：`go-server-final-authoritative.log`、`go-server-final-authoritative.exit`、`coverage-server-final-authoritative.out`、`coverage-server-final-authoritative-func.txt`、`coverage-gate-server-final.log`、`coverage-gate-server-final.exit`、`zz-latest-hardening-addendum.txt`。

## 8. 未通过：依赖与镜像安全

### S-01 Controller 镜像漏洞（源码候选已关闭，线上未部署）

原始候选为 Alpine 3.20.10 + Go 1.22.12，Trivy 发现 1 Critical、33 High，`govulncheck` 还发现调用链可达的 `GO-2026-5970`。本轮已完成并验证以下修改：

1. `go.mod` 提升到 Go 1.26，并固定 `toolchain go1.26.7`。
2. `golang.org/x/crypto` 从 v0.31.0 升到 v0.55.0、`x/sys` 从 v0.28.0 升到 v0.47.0、`x/text` 从 v0.21.0 升到 v0.41.0。
3. 构建层固定为 Go 1.26.7/Alpine 3.24 与 Node 24/Alpine 3.24 的 digest；运行层固定 Alpine 3.24.1 digest。
4. 新候选无缓存构建成功；`go mod verify`、vet、build、short 均通过。
5. `govulncheck ./...`：**0 个业务可达漏洞**（另有 2 个包级/2 个模块级 finding，但业务没有调用）。
6. Trivy `--severity HIGH,CRITICAL --ignore-unfixed --exit-code 1`：Alpine 层 **0**，Go binary **0**，命令退出 0。

结论：源码/候选镜像的 S-01 关闭。线上容器仍运行旧二进制，因此部署前必须重新从当前锁定源码构建、扫描并核对 digest，不能把本地 tag 当作线上已修复。

证据：`go-final-static-short-go1267.log`、`docker-build-final-secure.log`、`govulncheck-final.log`、`trivy-controller-final.log`、`controller-final.cdx.json`。

### S-02 Web 依赖漏洞（已关闭）

原结果为全依赖 8 个（1 Critical、2 High、5 Moderate），生产依赖 2 个 Moderate。已升级 `react-router-dom` 7.18.2、Vite 8.2.2、Vitest 4.1.11、`@vitejs/plugin-react` 6.1.0，并更新 lockfile；随后 npm 自动把残余 `nanoid` 更新到安全版本。

复验结果：`npm audit` **0 vulnerabilities**；Vitest 4 个文件/25 项通过；TypeScript + Vite 8 生产构建通过。三浏览器 E2E 仍属于第 10 节的环境阻断项，但依赖漏洞门禁已关闭。

证据：`web-audit-zero.log`、`web-test-upgraded.log`、`web-build-upgraded.log`。

### S-03 SillyTavern-online 生产依赖漏洞（已关闭）

原结果为 56 个漏洞（4 Critical、16 High、35 Moderate、1 Low）。前一轮已关闭 Critical/High，但留下 Showdown 三项和旧 Jimp/file-type 链共 6 个 Moderate。本轮进一步处理：

1. Showdown 固定为上游 commit `d1a8d16344b855ea2f37677ee8a9e0cc2c597ea4` 的 HTTPS archive，并由 lockfile SHA-512 integrity 约束；该 commit 包含 metadata title、table header 注入和 anchor parser ReDoS 修复。
2. 用 `@xenova/transformers` 2.17.2 替换 deprecated/broken fork，消除其 Jimp 0.22/file-type 16 链。
3. `onnxruntime-web` 固定并全局 override 为真实推理验证过的 1.16.1；sharp 固定/override 为已修复 2026 libvips 公告的 0.35.0。
4. sharp 0.35 要求 Node >=20.9，项目 engine 已同步提升；当前验收 Node 为 20.20.2。

复验：

- `npm audit --omit=dev`：**0 vulnerabilities**。
- 干净 `npm ci --ignore-scripts` 和 post-install：PASS。
- Showdown 的两类 XSS payload、公开 CVE ReDoS payload（9,999 次重复）及 SillyTavern extension 兼容：PASS；ReDoS 转换远低于 2 秒门禁。
- 真实 `Xenova/all-MiniLM-L6-v2` 下载/加载/推理：PASS，输出 `[1,384]` 且数值有限。
- sharp 0.35 生成的 RGBA PNG 经应用 `getRawImage`：PASS，得到 2×3×4。
- 六套 adapter 加两套依赖安全测试合计 53 pass、0 fail、0 skip；相关 ESLint 0 error；完整 SillyTavern 冷启动 + CSRF 跨仓测试 PASS。

结论：S-03 依赖审计门禁关闭。语音/图像模型的完整业务矩阵仍是兼容性加测项，但不再是已知 npm 漏洞；Node 18 环境不得部署此候选。

证据：`online-npm-audit-production-secure-final.log`、`online-transformer-real-inference.log`、`online-vectra-nodemailer-api-smoke.log`、`online-stcontrol-tests-final-secure-deps.log`、`online-full-cold-start-after-email-tls.log`、`zz-final-hardening-round-summary.txt`。

## 9. 未通过：静态检查、秘密、TLS、日志和主机

### S-04 adapter ESLint（本次范围已关闭）

原始结果为四个生产改动文件 73 个 error；测试 lint 还因 ESM 仓库中的 CommonJS `.eslintrc.js` 退出 2。本轮删除重复/错误的 `.eslintrc.js`，保留 `.eslintrc.cjs`，为 Node test 增加 ES2021 环境和适用规则；机械修复格式后，人工删除 unused import，并用 block 包住 `switch case` 的 lexical declarations。

最终对四个生产文件、六个 stcontrol 测试文件和两套依赖安全测试执行 ESLint：**0 error、退出 0**。格式修改后最终 Node 轮次为 53 pass。

证据：`online-stcontrol-eslint-fixed.log`、`online-stcontrol-tests-after-eslint.log`。

### S-05 Git 历史 secret 候选

Gitleaks 扫描 114 个提交，发现 4 个候选：3 个测试 fixture，1 个历史 `run-live-controller.bat`。

bat 候选：变量名为 `CONTROLLER_SECRET_KEY`，值长 44，无 placeholder 标记。安全的等值比较确认：

- 不等于当前运行 Controller secret。
- 不等于当前 `.env` secret。
- 当前 `.env` 与运行容器一致，且文件权限 0600。

仍必须按泄露处置：

1. 立即确认并轮换所有曾使用历史值的 Controller、备份、见证或恢复环境。
2. 将 bat 改为只从环境或 0600 文件读取，不再含任何默认 secret。
3. 使用 `git filter-repo --replace-text` 或等价工具清理历史；先备份、通知所有协作者，再协调 force-push 和所有 clone 重新同步。
4. 三个测试 fixture 用明确的非秘密生成器或带注释固定 fixture，并只对精确 fingerprint 做 allowlist。
5. 清理后重新扫描完整历史，要求 0 未处置 finding。

证据：`gitleaks-findings-safe.json`、`gitleaks-candidate-classification.txt`、`controller-secret-equality-only.txt`。原始报告已 100% redacted。

### S-06 TLS 1.2 与公网仍缺安全响应头

实际结果：

- Controller origin 8443：TLS 1.3 成功，TLS 1.2 被拒绝。
- Nginx 443：TLS 1.2 和 TLS 1.3 都成功。
- Cloudflare 公网边缘：TLS 1.2 和 TLS 1.3 都成功。
- 当前线上首页响应没有观察到 HSTS、CSP、`Referrer-Policy`、`X-Content-Type-Options`、frame/permissions policy。
- 本轮候选源码已经在应用层加入 F-12 的统一安全头并通过 success/error/404 测试，但线上仍是旧镜像，不能以源码测试替代公网验证。

修复：

1. Nginx 改为 `ssl_protocols TLSv1.3;`，执行 `nginx -t` 后平滑 reload。
2. Cloudflare 将 Minimum TLS Version 设置为 1.3，并关闭兼容 TLS 1.2 的策略。
3. 部署当前应用安全头后在 Nginx、origin 和公网逐层验证：HSTS、CSP、`Referrer-Policy: no-referrer`、`X-Content-Type-Options: nosniff`、frame deny、最小 Permissions-Policy。当前 CSP 仅因 React style attribute 对 CSS 保留 `'unsafe-inline'`，script 不允许 inline；后续应迁移 style attribute 后进一步收紧。
4. 重跑正确证书、错 hostname、过期、未知 CA、TLS 1.2 拒绝、TLS 1.3 成功矩阵。

证据：`nginx-tls12-handshake.txt`、`nginx-tls13-handshake.txt`、`public-edge-tls12.txt`、`public-edge-tls13.txt`、`nginx-public-headers.txt`、`public-edge-headers.txt`。

### S-07 Nginx query 日志

18,512 条 Nginx access log 中有 887 条带 query；没有使用 redacted query 格式。参数名统计出现 `username` 和 `psd` 各 15 次。即使它们来自恶意扫描流量，也证明 access log 会原样落 query 值。

修复：

1. 自定义 `log_format`，请求目标只记录 `$uri`，不要记录 `$request` 或 `$request_uri`；需要业务诊断的非敏感参数应由应用显式、白名单化、脱敏记录。
2. reload 后用生成的非秘密 marker 验证 access log 中没有 query 值。
3. 将现有日志视为可能含敏感参数，限制权限、缩短保留期并按安全流程轮换/销毁；不要把原日志上传到普通 CI artifact。
4. Controller 自身已把 200 条敏感 query 显示为 `redacted`，应保持该行为。

证据：`nginx-log-config.txt`、`nginx-log-sensitive-summary.txt`、`nginx-query-key-counts.txt`、`stcontrol-controller-1-log-sensitive-summary.txt`。

### S-08 系统时钟未同步

`timedatectl`：`System clock synchronized: no`、`NTP service: n/a`；RTC 与 UTC 观察值约差 7 秒。清单要求节点误差小于 2 秒。

修复：安装并启用 chrony 或 systemd-timesyncd，配置至少 3 个可靠时间源；等待 `tracking`/`timedatectl` 确认 synchronized，再在所有 Controller、Agent、数据库节点记录最大 offset < 2 秒。

证据：`host-time-ports-containers.txt`。

## 10. 未完成/被环境阻断的清单项

以下项目不能由当前“单服务器 + 一个生产 Controller + 一个生产 PostgreSQL + 本机临时进程”的环境证明，不能标记为 PASS：

1. 三台独立 compute Agent、pure-storage Agent、独立主动/被动 Controller 的真实网络拓扑。
2. NAT、跨主机证书 hostname、错 CA/过期证书和 Agent 数据面的真实握手矩阵。
3. 使用 `tc` 注入延迟、丢包、乱序、抖动后的 managed/independent 稳定性。
4. 三节点见证仲裁、owner Agent 整机丢失、A/B 真实网络分叉、被动双主竞争。
5. 1,000 个带独立来源和会话的并发登录/心跳；当前只有静态入口和数据库容量测试。
6. 大文件限速占满数据面时，heartbeat、login、admin API、command ACK 的并行延迟图表。
7. LinuxDo/Discord 的真实 provider、guild、双浏览器 state 绑定测试。
8. Chromium、Firefox、WebKit 的登录/注册/账号/管理/恢复/冲突全链路，320–1440 px 响应式截图、HAR、console。
9. 72 小时 soak、每角色三轮滚动重启、PostgreSQL 重启/切换、上一发布版本顺序升级。

补测环境至少应包含：1 主 Controller、1 被动 Controller、3 compute Agent、1 pure-storage Agent、独立 PostgreSQL 测试实例、可控网络故障注入机、浏览器执行机和指标采集。所有数据必须为生成数据。

## 11. 发布前精确修复顺序

### P0：修复后才能继续验收

1. 处置 Git 历史 Controller secret：先轮换，再清历史，再重新扫描。
2. 将修复后权威严格全仓覆盖率从 74.7%（13,263/17,756）提升到至少 80%，至少再覆盖 942 条当前未执行 statement。
3. 修复 Nginx query 日志，安全处理既有日志。
4. 仅部署从当前 Go 1.26.7/Alpine 3.24.1 和 Node >=20.9 锁定配置重建且扫描为 0 Critical/High 的候选；线上旧容器不视为已修复。

已关闭：Controller 原 1 Critical/33 High、可达 govulncheck finding、Web 8 个漏洞、adapter 原 56 个生产依赖漏洞和本次 adapter ESLint error。

### P1：在候选发布环境关闭

1. Nginx 与 Cloudflare 只允许 TLS 1.3，增加安全响应头。
2. 启用时间同步并证明所有节点 offset < 2 秒。
3. 在安全的新候选镜像中包含 F-01～F-22 修复；部署后确认 production SQL ambiguous error 为 0，并逐层验证安全响应头。
4. 生成 SBOM，保存镜像 digest、Go build info、npm lockfile 和扫描数据库时间。

### P1：补齐验收证据

1. 完成真实多节点灾难/分区/NAT/双主测试。
2. 完成 1,000 并发登录/心跳和控制/数据面隔离压测，记录 P50/P95/P99、错误率和系统曲线。
3. 完成 Chromium/Firefox/WebKit 和响应式矩阵。
4. 完成 72 小时 soak、滚动重启、数据库切换和上一版本升级。

### 全量复验命令

修复后至少重新执行：

```bash
go mod verify
go vet ./...
go build ./...
go test -short -count=1 ./...

STCONTROL_TEST_POSTGRES_DSN='REDACTED' \
STCONTROL_TEST_PG_BIN="$(command -v pg_dump)" \
STCONTROL_TEST_TAVERN_ROOT='/path/to/fixed/Sillytarven-online' \
go test -p 1 -count=1 -v ./...

go test -p 1 -race -count=1 ./...
go test -p 1 -count=1 -coverpkg=./... -covermode=atomic \
  -coverprofile=coverage.out ./...
go tool cover -func=coverage.out

govulncheck ./...
trivy image --exit-code 1 --severity HIGH,CRITICAL --ignore-unfixed IMAGE
npm audit --omit=dev
```

然后执行第 10 节的真实拓扑、性能、浏览器和长稳测试。任何一项 skip、blocked、Critical/High、覆盖率低于 80 或证据缺失，都不得签署发布。

## 12. 线上状态与部署注意事项

运行中 Controller 二进制 SHA256：`2ae9ca4d...b2ce28`。

最终验收镜像 ID：`sha256:c81469658bbe630dd9dc79b26849f24b2e28b1f4a5e8ea81fec8fc1280d1c807`。

最终验收镜像 Controller 二进制 SHA256：`b9a3e73d1fd9b6a5abcff6352830637495e99f513688680d7cf1332665430664`。

两者不同，说明线上仍为旧版本。线上镜像 ID 与验收镜像 ID 也不同。当前生产资源占用较低，PostgreSQL 17.10 可连接，未发现数据库 deadlock；但运行日志中的 `ListUsers` ambiguous SQL 会持续发生，且线上没有本轮事务安全修复。

在 P0/P1 关闭前不要部署。关闭后应采用：

1. 先备份并验证 PostgreSQL 恢复与 master-key recovery。
2. 构建固定 digest、扫描通过的新镜像。
3. 在隔离 staging 完整复验。
4. 以滚动/可回退方式部署，先 Controller 后按 generation fence 轮换 Agent 凭据。
5. 观察 SQL error、保护投影错误、heartbeat、command queue、内存/FD 和告警至少一个完整业务周期。
6. 只有新产物 digest、迁移版本、运行二进制 build info 和测试报告全部一致时才关闭升级项。

证据：`controller-artifact-comparison.txt`、`docker-stats.txt`、`production-postgres-readonly-health.txt`、`postgres-recent-error-counts.txt`。

## 13. 最终签署

最终结论：**FAIL / NOT PRODUCTION READY**

测试负责人：Codex 自动化验收（未代替人工签字）

发布负责人：待 P0/P1 全部关闭、完整复验后签字
